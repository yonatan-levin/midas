# DC-1 — Datacleaner adjusters are single-sided; collapse "valuation overlay" with "as-of restatement"

**Status:** CLOSED / RESOLVED 2026-06-02 — DC-1 (all phases 0–5) is CODE-COMPLETE; Phase 5 follow-up merged `8ca0841`. The ONLY remaining DC-1 item is an OPERATOR follow-up (fresh CalcVersion-4.4 replay baseline capture + live DebtLikeClaims-EV confirmation on a B-rule-firing bank), tracked at `docs/reviewer/DC-1-phase-5-replay-verification-followup.md` — NOT a code blocker. Per-phase history retained below. **Phase 0 SHIPPED 2026-05-16** (merge `1640394`); **Phase 1 SHIPPED 2026-05-19** (merge `2d916a7`); **Phase 2 SHIPPED 2026-05-23** (merge `3238d61`); **Phase 3 MERGED 2026-05-25 as `46e84b1`**; **Phase 3 followup MERGED 2026-05-26 as `3490227`** (closes 9 cross-model gpt-5.5 review findings — HIGH-1 view-seed double-count, HIGH-2/3 B3 single-AI-call + ctx, MEDIUM-1 B1 ctx, MEDIUM-2 prompt-hash spec, LOW-1..4); **Phase 4 MERGED to master 2026-05-27 as `ce94f70`** (8 commits: C-1..C-5 `f65604a`/`4f8a06c`/`9bc885e`/`7349a1e`/`ae9113a` + `e521c53` NWC→AsReported fix + `2ea9978` revenue_multiple DebtLikeClaims fix + `3931a41` docs) — 13-site consumer migration + B3 routing flip + dispatcher dual-write deletion (§8.2.1 Option A) + `CalculationVersion` 4.2→4.3; 4 review rounds (incl. gpt-5.5 cross-model) caught + fixed 2 regressions (NWC recomputed-umbrella drift on AMD-class plug shortfalls; revenue_multiple silently dropped B1/B2/B3 DebtLikeClaims). **Phase 5 spec+plan AUTHORED on `dc1-phase-5-prep`** (`docs/refactoring/spec/datacleaner-component-primitive-and-parallel-views-phase-5-spec.md` + `docs/refactoring/implementations/datacleaner-component-primitive-and-parallel-views-phase-5-implementation-plan.md`; CalcVersion → 4.4). Phase 5 scope: DDM consumer migration (`ddm.go` — deferred for bit-for-bit), `cleaneddata.Raw()` deletion (zero production callers verified), per-rule translator + `*AdjustmentResult` struct deletion (still load-bearing — orchestrator aggregates `result.Applied/.Adjustments/.Flags`), optional `sync.Once` accessor retrofit, optional stop-populating the legacy `historicalData` slot. Operator follow-up: fresh `4.2`/config-current replay baseline capture + Phase-4 drift attribution (the `2026-05-19` baseline is `calc_version 4.1`, confounding drift across phases). Phase 4 closeout: `docs/refactoring/implementations/datacleaner-component-primitive-and-parallel-views-phase-4-closeout.md`. **Phase 5 SHIPPED — PARTIAL merged `e816fcc`, follow-up merged `8ca0841` (2026-06-02); DC-1 CLOSED.** The PARTIAL (10 commits, tip `e6418e4`) shipped P5-C1 (DDM EV-bridge `+DebtLikeClaims` + CalcVersion 4.3→4.4; DDM-specific ADDED sign — opposite of DCF/revenue_multiple — because DDM derives equity FROM dividends THEN EV from equity) / P5-C2 (DDM consumer migration to `Restated()` via `LatestRestatedView` in `runDividendDiagnostics` + `estimateDividendGrowth` with 4-step in-commit bit-for-bit re-proof; JPM/BAC/WFC math preserved) / P5-C3 SCOPED (orchestrator firing-signal migrated to `nativeFired(entries, overlays, flags)` helper at `internal/services/datacleaner/firing_signal.go` filtering `LedgerEntry.Fired==true`) / P5-C5 PARTIAL (`cleaneddata.Raw()` deleted + concurrency contract godoc strengthened to HARD request-local invariant + `historicalData` slot KEEP via `keepLatestCleanedSlot` helper). The follow-up (merged `8ca0841`; branch `dc1-phase-5-followup` — 7-commit ladder A0–C1 + 4 review-fix commits FIX-1..FIX-4 + closeout/operator-followup docs) CLOSED the three deferred CODE chunks: **P5-C3-full Adjustments-projection** — `result.Adjustments` now produced by the native `adjustmentsFromLedger(...)` projection at `internal/services/datacleaner/adjustment_projection.go` (ARCH Path (a): preserve `Adjustment.Percentage` bit-for-bit via fired-rule pre-state on `LedgerEntry.SkipMetrics["original_<Field>"]`; ARCH note `docs/refactoring/spec/dc1-phase-5-followup-percentage-decision.md`); **P5-C4 deletion** — the 16 `*AdjusterOutputToLegacyResult` translators + 3 category result structs (slimmed) + dormant `earnings.go` fallback helpers + dead `entities.{Asset,Liability}AdjustmentResult` duplicates + 9 test-only-dead `Process*Adjustment` helpers DELETED; **DDM `modelIBD` view flip** — reads `Restated().InterestBearingDebt`. Validation: full `/execute` B-V-R-Q ×2 + gpt-5.5 cross-model Q-pass ×2 (no Critical/High); `/verify` runtime replay confirmed `calculation_version "4.4"` live. **The former "translator stack still load-bearing for `result.Adjustments` aggregation" note is now FALSE and RETIRED:** the per-rule translators are DELETED; `result.Adjustments` is produced solely by the native `adjustmentsFromLedger` projection over `data.AdjustmentLedger`. **ONLY remaining (OPERATOR, NOT a code blocker):** fresh CalcVersion-4.4 replay baseline capture + live DebtLikeClaims-EV confirmation on a B-rule-firing bank — tracked at `docs/reviewer/DC-1-phase-5-replay-verification-followup.md`. Follow-up closeout: `docs/refactoring/implementations/dc1-phase-5-followup-closeout.md`. Phase 5 PARTIAL closeout: `docs/refactoring/implementations/datacleaner-component-primitive-and-parallel-views-phase-5-closeout.md`. Originally filed 2026-05-05 during the Graham-floor metrics design pass.
**Severity:** Major (silently produces inconsistent balance-sheet output; surfaces only when a downstream consumer needs `Assets = Liabilities + Equity` to balance).
**Origin:** Investigation triggered by `docs/refactoring/archive/graham-floor-metrics-spec.md` (R2 risk discussion). Discovered while validating whether to derive `TotalLiabilities` from `TotalAssets − StockholdersEquity` for NCAV.
**Blocks:** No production work. Graham-floor metrics ship around it via direct `us-gaap:Liabilities` XBRL preference (see `graham-floor-metrics-spec.md` §4.4).
**Related specs:** `docs/refactoring/archive/graham-floor-metrics-spec.md`, `docs/refactoring/spec/industry-classification-unification-spec.md` (similar "two parallel paths" theme).
**Phase 0 progress (2026-05-16):** Added 4 plug fields to `FinancialData` (`OtherCurrentAssets`, `OtherNonCurrentAssets`, `OtherCurrentLiabilities`, `OtherNonCurrentLiabilities`); SEC parser populates them at end of `parsePeriodData` via `computePlugs`. Empirically zero behavior change (replay-verified on AAPL + MSFT, timestamp-only drift). Property test + ticker-basket integration test + persistence round-trip pin the components-sum-to-umbrellas invariant. SQLite-side schema migration deferred to Phase 1+ via a flip-gate test. Phase 1 (`recomputeUmbrellas` shadow shim) is now unblocked. See:
- Phase 0 closeout report — `docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-0-closeout.md` — comprehensive summary of what landed, empirical verification, what's deferred to Phase 1+, and lessons learned.
- Design spec — `docs/refactoring/spec/datacleaner-component-primitive-and-parallel-views-spec.md`
- Phase 0 implementation plan — `docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-0-implementation-plan.md`

**Phase 1 progress (2026-05-19):** Added `internal/services/datacleaner/recompute.go::recomputeUmbrellas` — a pure shadow-mode observer wired in at the end of `CleanFinancialData` (between `createRiskWarningFlags` and `calculateQualityScore`). The function recomputes each balance-sheet umbrella from `sum(known_components) + plug` and emits a structured WARN log (`phase: "DC-1-P1-shadow"`) on divergence WITHOUT mutating `*FinancialData`. The no-mutation invariant is pinned by `TestRecomputeUmbrellas_NoMutation`'s `reflect.DeepEqual` snapshot; the well-formed-input baseline is pinned by `TestRecomputeUmbrellas_Property_WellFormedNoDivergence` (gopter, 4 properties × 200 iterations, seed `20260517`). The basket integration test at `internal/integration/datacleaner_recompute_shadow_test.go` runs the full cleaner pipeline across the 10-ticker basket and records every divergence as committed JSON snapshots under `internal/integration/testdata/recompute-shadow/<TICKER>.json` — these snapshots are the Phase 1 → Phase 2 hand-off artifact for the targeted-fix punch list. Recording-not-asserting on divergence count (known clamp-fired periods MXL 2017FY / EQIX 2013Q1 produce expected WARN lines); the single load-bearing integration assertion is `passedCount >= 5`. Zero downstream behavior change: full datacleaner suite green. Phases 2-4 (`Adjuster` interface refactor → `CleanedFinancialData` view system → consumer migration) follow after the post-merge shadow-analysis report enumerates the divergent (ticker, period, umbrella) tuples and confirms no unexplained patterns. See:
- **➡️ Phase 1 implementation plan** — `docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-1-implementation-plan.md` — the authoritative ARCH-approved plan that drove this phase.
- Phase 1 implementer handoff — `docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-1-handoff.md` — the originating handoff.
- **Phase 1 closeout report** — `docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-1-closeout.md` — filed post-merge. Comprehensive summary of what landed, V-R-Q chain verdicts (REVIEWER 9 findings all addressed in follow-up PR), empirical verification, lessons learned, and 11/11 acceptance criteria status.
- **Shadow-analysis report** — `docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-1-shadow-analysis.md` — filed post-merge. The Phase 1 → Phase 2 gate input. Enumerates 142 divergence records across 7 basket tickers into 7 distinct clusters with explicit Phase 2 dispositions. **Phase 2 gate verdict: SATISFIED.** Surfaces a parser-side prerequisite (AMD + KO `TotalLiabilities = 0` dropout, 24 records) for Phase 2's regression-validation basket — filed as `docs/reviewer/T2-BS-3-parser-totalliabilities-zero-amd-ko.md` for independent tracking.
- **Basket coverage refreshed (2026-05-19)** — `artifacts/tier2-baseline/2026-05-19/` captured with all 10 basket tickers (AAPL, AMD, BABA, EQIX, F, JNJ, KO, MSFT, MXL, TSM); shadow integration test runs across full 10/10 (was 7/10 on the 2026-05-15 baseline). Closes the JNJ/TSM/BABA absence flagged in the shadow-analysis report §7. The 7 pre-existing snapshot files updated for `bundle_root`/`fixture_path` metadata only; divergence records byte-stable. 3 new snapshots (JNJ.json: 23 divergences across 12 periods; TSM.json: 2 divergences across 2 periods; BABA.json: 3 divergences across 3 periods) expand Phase 2's punch-list signal.
- **➡️ Phase 2 implementer handoff filed** — `docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-2-handoff.md` — READY TO START. Covers scope (Adjuster interface + LedgerEntry + refactor of 8+ adjusters; no view reconstruction), the T2-BS-3 disposition decision gate, the 7-cluster punch list from Phase 1's shadow analysis, snapshot files as the regression signal, and a starting prompt. Estimated 2-3 weeks (Layer 2 of the spec, materially larger than Phase 1's shadow-mode shim).

**Phase 2 PR-1 progress (2026-05-21):** Skeleton landed on branch `dc1-phase-2-pr-1-clean` (pending VERIFIER → REVIEWER → QA → HUMAN merge). PR-1 introduces:
1. `Adjuster` interface and `AdjusterOutput` struct at `internal/services/datacleaner/adjustments/adjuster.go` — unified contract for every cleaner-side adjuster; Restater / OverlayEmitter / Hybrid roles emerge from output shape, not interface multiplication.
2. `LedgerEntry`, `OverlaySpec`, `AdjustmentLedger`, `AmountSemantics`, `AIProvenance` entities at `internal/core/entities/adjustment_ledger.go`. `LedgerEntry.SourceReliability` is the T2-BS-3 carve-out hook.
3. `AdjustmentLedger []LedgerEntry` and `Overlays []OverlaySpec` fields appended to `entities.FinancialData` (positioned after the Phase 0 plug fields).
4. Orchestrator scaffolding shim at `service.go::applyActiveAdjustments` — mechanically maps the legacy `entities.Adjustment` shape to `LedgerEntry` records after each `Process*Adjustments` call. Three contiguous shim branches (assets / liabilities / earnings) are deletion order: PR-2 deletes assets, PR-3 deletes earnings, PR-4 deletes liabilities, as each category's adjusters implement `Adjuster.Apply` natively.
5. `TestOrchestrator_LedgerOrdering` property test at `internal/services/datacleaner/ledger_invariants_test.go` pinning the asset → liability → earnings partition (deviates from plan's `adjustments/` location due to import-cycle; intent preserved by using the real datacleaner service).
6. `recomputeUmbrellas` WARN line additively renders `recent_adjusters []string` field (last 5 AdjusterIDs from `fd.AdjustmentLedger`) per Q1 resolution 2026-05-21. The load-bearing `TestRecomputeUmbrellas_NoMutation` invariant is preserved.

PR-1 invariant: **opt-in observability only — no production consumer reads the ledger or overlays yet** (matches Phase 0 plug-field discipline). The existing dual-write mutations (`data.TotalAssets -= X`, `data.TotalDebt += Y`, etc.) remain unchanged; downstream DCF / WACC / Graham / EV-bridge outputs are bit-for-bit unchanged. `TestDDM_LegacyPath_BitForBit` stays GREEN. Subsequent PRs (2 → 3 → 4) migrate one adjuster category at a time atop this skeleton.

Plan: `docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-2-implementation-plan.md` (4-PR strategy with Q1 SHIP / Q3 INCLUDE / Q4 ACCEPT resolutions baked in).

**Phase 2 PR-3 SHIPPED (2026-05-22, branch `dc1-phase-2-pr-3` — pending merge, stacked on PR-2 tip `2e8f83b`):** All 7 Category C earnings adjusters migrated to the `Adjuster` interface natively, and the earnings-side branch of PR-1's `shimLedgerEntriesFromLegacyExcluding` deleted. Eight commits landed on branch `dc1-phase-2-pr-3`: Task 3.1 (C1 restructuring_charges, Restater) `b1af6b1`; Task 3.2 (C2 asset_sale_gains, Restater) `e621320`; Task 3.3 (C3 litigation_settlements, Restater) `988a371`; Task 3.5 (C5 derivative_gains_losses, Restater with branch-divergent sign) `5654464`; Task 3.6 (C6 capitalized_interest, Restater with LOAD-BEARING `EquityOffset=0` special case for interest reclassification — Phase 3 `Restated()` must NOT add C6's DeltaAmount to retained earnings) `5610d51`; Task 3.4 (C4 stock_compensation, FlagEmitter — plan-vs-code disagreement resolved by trust-the-code precedent: the plan §7 row described C4 as "same pattern as C1" implying Restater, but the legacy code at `ProcessStockCompensationAdjustment` does NOT mutate `data.NormalizedOperatingIncome`; it only emits an `entities.Adjustment{Type:Reclassify}` and a dilution `entities.Flag`, so C4 ships as FlagEmitter following PR-2 Task 2.5's convention) `79b78bd`; Task 3.7 (C7 working_capital_window_dressing, FlagEmitter) `75afa8b`; Task 3.8 (delete earnings-side shim branch in `service.go::applyActiveAdjustments`, mirroring PR-2's Task 2.6 asset-side deletion) `4af3c33`. **Two role flavors used (both already locked by PR-2):** Restater (C1/C2/C3/C5/C6) and FlagEmitter convention (C4/C7) — no new flavors required. The two notable per-rule special cases: (a) **C5 branch-divergent sign** — the legacy `ProcessDerivativeGainsLossesAdjustment` has two mutation sites at `earnings.go:313/316` that look duplicated but live in gain-path vs loss-path branches; `adjustmentAmount` carries the sign through both. The Adjuster emits ONE LedgerEntry with signed `DeltaAmount = -DerivativeGainsLosses` (gain-path NEGATIVE = subtract gain, loss-path POSITIVE = add back loss). The dispatcher reads the signed DeltaAmount off the native LedgerEntry directly because the translator absolute-magnitudes for the legacy `Adjustment.Amount` field — branch-identity sign would be lost via the translator path. (b) **C6 EquityOffset=0** — capitalized interest reclassifies between income-statement line items (operating expense → interest expense), does NOT flow to retained earnings; pinned by a dedicated subtest assertion in `c6_capitalized_interest_adjuster_test.go` AND by the dispatcher test's `NativeC6Emission` subtest with a failure message that explicitly names "Phase 3 Restated() must NOT add C6 DeltaAmount to retained earnings". Canonical Phase 2 pattern unchanged across all 7 C-rule migrations: mutation-FREE `Apply*` on `EarningsAdjuster`; the dispatcher `ProcessEarningsAdjustments` switch owns the dual-write (`data.NormalizedOperatingIncome ±=` or `data.InterestExpense +=`) + drain-natives. **`CurrentSchemaVersions["FinancialData"]` stays at 8** (atomic bump landed in PR-2 Task 2.1 — PR-3 reuses the same structural envelope). **Shim helpers `shimLedgerEntriesFromLegacy` and `shimLedgerEntriesFromLegacyExcluding` PRESERVED** for PR-4 (which deletes them alongside B-rule migration; the liability-side shim branch still calls `shimLedgerEntriesFromLegacy`). **Translator-string discipline (PR-2 Task 2.1 NIT carry-through):** C3/C5/C6 add a `Revenue > 0` percentage guard that diverges from legacy's NaN/+Inf on Revenue=0 tickers — an intentional defensive guard documented per-commit; otherwise legacy `Reasoning` strings preserved byte-identically where practical. Load-bearing invariants stayed GREEN throughout all 8 PR-3 commits: `TestDDM_LegacyPath_BitForBit` (jpm/bac/wfc — trivially preserved; DDM doesn't read earnings-normalization fields), `TestRecomputeUmbrellas_NoMutation`, `TestOrchestrator_LedgerOrdering` (asset → liability → earnings partition preserved; C-rule LedgerEntries appear in the earnings partition), `TestDataCleanerRecompute_ShadowMode_TickerBasket` + shadow-snapshot byte-identity (predicted-zero per plan §4 row C; empirically confirmed via `git diff --quiet internal/integration/testdata/recompute-shadow/` exit 0 after each commit). PR-4 (B1/B2/B3 liability adjusters with B3 reroute to `DebtLikeClaims` — NOT TotalDebt — substantive accuracy correction; orchestrator-level `data.TotalDebt += result.Amount` absorption at `liabilities.go:87-88`; final shim + helper deletion; basket snapshot integration test; T2-BS-3 disposition documentation; Phase 2 closing CLAUDE.md gotcha) is the next handoff — see `docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-2-pr-4-handoff.md`. PR-4 is the highest-risk PR in the Phase 2 stack.

**Phase 2 PR-2 progress (2026-05-22, branch `dc1-phase-2-pr-2` — pending merge):** All 6 Category A adjusters migrated to the `Adjuster` interface natively, and the asset-side branch of PR-1's `shimLedgerEntriesFromLegacyExcluding` deleted. Seven commits landed on branch `dc1-phase-2-pr-2` (stacked on PR-1 tip `dc1-phase-2-pr-1-clean`): Task 2.1 (A1 OverlayEmitter + SchemaVersion 7→8) `4ca4b3c`; Task 2.2 (A2 Restater) `15a5798`; Task 2.3 (A4 Restater) `79d3015`; Task 2.4 (A5 Restater + TaxShieldDTA) `631bf72`; Task 2.5 (2 flag-only reviews) `039b680`; Task 2.6 (delete asset-side shim) `2c132aa`; Task 2.7 (A-FY-NULL read-only tracker at `docs/reviewer/DC-1-FY-enable-predicate-investigation.md`) `df25866`. Four role flavors emerged from the output shape: **OverlayEmitter** (A1 — empty Component on LedgerEntry, monetary delta lives on `OverlaySpec.Amount`); **Restater** (A2/A4 — `Component`/`DeltaAmount<0`/`EquityOffset=DeltaAmount`); **Restater + TaxShieldDTA** (A5 — populates `TaxShieldDTA = writedown × working.EffectiveTaxRate` when EffectiveTaxRate > 0); and **FlagEmitter convention** (2 reviews — `Fired:false` LedgerEntry with non-empty `AdjusterOutput.Flags` as the firing signal). Canonical pattern locked across all 6 migrations: mutation-FREE `Apply*` on `AssetAdjuster`; the dispatcher `ProcessAssetAdjustments` switch owns the dual-write (capture original* → Apply → translate to legacy `*AdjustmentResult` via per-rule translator → mutate `data.X` → drain `AdjusterOutput.LedgerEntries`/`Overlays`/`Flags` into result's `Native*` slices + add the RuleID to `NativelyEmittedRuleIDs`). **`CurrentSchemaVersions["FinancialData"]` bumped 7→8 atomic with Task 2.1** per `feedback_schema_version_atomic_bump` (first populating PR rule); `artifacts/tier2-baseline/2026-05-19/` baseline refresh deferred to a follow-up commit (requires live API). The A-FY-NULL finding has its own tracker at `docs/reviewer/DC-1-FY-enable-predicate-investigation.md` (HIGH-confidence NOT-A-BUG for Phase 2; rooted in SEC XBRL FY-annual vs Qx-quarterly Revenue magnitudes causing `InventoryTurnover` to land ~4× higher on FY, dropping FY periods out of A5's quarterly-tuned `<3.0` obsolescence threshold; heuristic fix punted to a Phase 4+ "FY-aware annualization for quarterly-tuned heuristics" subtask). Load-bearing invariants stayed GREEN throughout: `TestDDM_LegacyPath_BitForBit` (jpm/bac/wfc), `TestRecomputeUmbrellas_NoMutation`, `TestOrchestrator_LedgerOrdering`, `TestDataCleanerRecompute_ShadowMode_TickerBasket` + shadow-snapshot byte-identity (`git diff --quiet internal/integration/testdata/recompute-shadow/` exit 0). Q2 (A2 TaxShieldDTA) and Q4 (AIProvenance hash fields) remain DEFERRED to Phase 3 by design. PR-3 (earnings adjusters C1-C7 + Task 3.8 earnings-side shim deletion) is the next handoff — see `docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-2-pr-3-handoff.md`. PR-4 (B1/B2/B3 liability adjusters + final shim deletion + T2-BS-3 docs + Phase 2 closing CLAUDE.md gotcha) closes Phase 2.

**Phase 2 SHIPPED (2026-05-23, branch `dc1-phase-2-pr-4` — full 4-PR stack awaiting final HUMAN merge to master):** All 17 cleaner-side adjusters now implement the `Adjuster` interface natively. The 4-PR stack landed in stack order on independent branches: PR-1 `dc1-phase-2-pr-1-clean` tip `39cf0fa` (interface + entities + scaffolding shim); PR-2 `dc1-phase-2-pr-2` tip `2e8f83b` (6 A-rules + asset-shim deletion + SchemaVersion 7→8); PR-3 `dc1-phase-2-pr-3` tip `207f41a` (7 C-rules + earnings-shim deletion); PR-4 `dc1-phase-2-pr-4` final tip (3 B-rules + orchestrator absorption + liability-shim deletion + shim-helper deletion + basket integration test + Phase 2 closeout docs sweep). **PR-4's 7 commits:** Task 4.1 B1 operating_lease_capitalization OverlayEmitter (`Field:"TotalDebt"`) `613773a`; Task 4.2 B2 pension_underfunding OverlayEmitter (`Field:"TotalDebt"`) `e328226`; Task 4.3 B3 contingent_liability OverlayEmitter (**`Field:"DebtLikeClaims"`** — Phase 4 routing intent; today's Phase 2 dual-write still mutates `data.TotalDebt`) with best-effort `AIProvenance` (`ModelName: b3AIModelName`; `PromptHash`/`SourceDocHash`/`ExtractedSpan` empty per Q4 deferral) `5c4e88d`; Task 4.4 orchestrator `data.TotalDebt += result.Amount` absorbed into `ProcessLiabilityAdjustments`' per-rule switch arms (Option α — keep wrapper, move mutation into wrapper) `37f7400`; Task 4.5 liability-side shim branch deleted + both shim helpers (`shimLedgerEntriesFromLegacy` + `shimLedgerEntriesFromLegacyExcluding`) removed — **PR-1 shim FULLY gone** `5eaadd3`; Task 4.6 `TestLedger_BasketSnapshot_ClusterPrediction` integration test at `internal/integration/datacleaner_ledger_basket_test.go` (10/10 basket tickers PASS — first integration test to read `data.AdjustmentLedger` directly and assert on per-ticker expected AdjusterID sets, derived from the shadow-analysis cluster mapping inverted via `service.go::checkRuleApplicability`) `f2c5d0e`; Task 4.7 Phase 2 closeout docs sweep — CLAUDE.md per-PR sub-bullets consolidated to a single Phase 2 SHIPPED bullet + spec/plan changelogs + this DC-1 tracker progress paragraph + T2-BS-3 Option B carve-out disposition + THESIS/AGENTS/ARCHITECTURE/TESTING refreshes + NEW Phase 2 closeout doc at `docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-2-closeout.md`. **Canonical pattern locked across all 17 adjusters:** mutation-FREE `Apply*` methods (read `working`, return `AdjusterOutput`); the dispatcher `Process*Adjustments` switch owns the dual-write (capture original* → call `Apply*` → translate to legacy `*AdjustmentResult` via per-rule translator → mutate `data.X` → drain `AdjusterOutput.LedgerEntries`/`Overlays`/`Flags` into result's `Native*` slices + add the `RuleID` to `NativelyEmittedRuleIDs`); the cleaner orchestrator drains the per-category `NativeLedgerEntries`/`NativeOverlays` directly onto `data.AdjustmentLedger`/`data.Overlays`; native-drain uses `NativelyEmittedRuleIDs` as the exclusion set. **Four role flavors canonized across all 3 categories:** OverlayEmitter (A1, B1, B2, B3), Restater (A2, A4, C1, C2, C3, C5, C6 — with C6's LOAD-BEARING `EquityOffset=0` special case for interest reclassification), Restater + TaxShieldDTA (A5 only), FlagEmitter (C4, C7, plus 2 PR-2 reviews). **Q-disposition final:** Q1 SHIPPED in PR-1; Q2 DEFERRED to Phase 3 (A2 TaxShieldDTA); Q3 SHIPPED in PR-2 Task 2.7 (A-FY-NULL tracker); Q4 DEFERRED to Phase 3 (`AIProvenance` SHA-256 hashes — zero ship with TODO; today's `ai.AnalyzeFootnote` call site does not compute them). **T2-BS-3 disposition:** Option B (carve-out) — parser fix DEFERRED; AMD/KO `AsReported.TotalLiabilities=0` stays; Phase 3's `Restated` view reconstruction (from `sum(components) + plug`) will produce a non-zero value, correctly surfacing the components-derived liability total for downstream consumption; tracker stays OPEN. **`CurrentSchemaVersions["FinancialData"]` at 8** (atomic bump landed in PR-2 Task 2.1; PR-3 and PR-4 reuse the same structural envelope). **Substantive accuracy correction now visible in the cleaner output:** B3's `OverlaySpec.Field:"DebtLikeClaims"` records Phase 4's routing intent — contingent liabilities feed `InvestedCapital.WACCInputs.DebtLikeClaims`, NOT `Restated.TotalDebt`. **Load-bearing invariants stayed GREEN throughout all 28 commits across the 4-PR stack:** `TestDDM_LegacyPath_BitForBit` (JPM/BAC/WFC — non-trivially preserved in PR-4 because B-rules touch `TotalDebt`; pinned by per-commit replay validation), `TestRecomputeUmbrellas_NoMutation`, `TestOrchestrator_LedgerOrdering` (asset → liability → earnings partition), `TestDataCleanerRecompute_ShadowMode_TickerBasket` + shadow-snapshot byte-identity, and the new `TestLedger_BasketSnapshot_ClusterPrediction`. **Phase 3 (CleanedFinancialData view reconstruction — `AsReported`/`Restated`/`InvestedCapital` accessors) is the next architectural gate;** Phase 4 (consumer migration of 13 valuation read sites + B3 routing flip + dual-write deletion) follows. Closeout: `docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-2-closeout.md`.

**Phase 3 spec authored 2026-05-23** by Phase 2 closeout ARCH. Deliverables: full design spec at `docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-3-spec.md` + implementer plan at `docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-3-implementation-plan.md`. Scope: introduce `internal/services/datacleaner/cleaneddata.CleanedFinancialData` with three lazy view accessors (`AsReported()` / `Restated()` / `InvestedCapital()`); resolve Q2 (A2 `TaxShieldDTA = writedownAmount × EffectiveTaxRate` mirroring A5's Phase 2 pattern); resolve Q4 (B3 `AIProvenance.PromptHash` + `SourceDocHash` as SHA-256 hex of rendered-prompt + footnote-text); thread `ctx context.Context` through `ProcessLiabilityAdjustments` + symmetric asset/earnings sibling signatures; lock the translator-extraction decision (KEEP per-rule; Phase 4 deletes alongside dual-write). T2-BS-3 carve-out surfaces in `Restated()` — AMD/KO get a truthful `Restated.TotalLiabilities` via component-sum reconstruction while `AsReported.TotalLiabilities` stays at 0 (parser fix remains deferred per Option B). **Phase 3 ships zero downstream behavior change**: the accessor surface is additive; no consumer migrates (Phase 4 owns that). Single-PR strategy recommended (1–2 agent shifts); Option B 2-PR split documented as a fallback. SchemaVersion bumps 8 → 9 atomic with Q4's first populating commit. Phase 3 → Phase 4 gate: all load-bearing Phase 2 invariants stay GREEN; AMD/KO `Restated().TotalLiabilities > 0` test pin; basket integration test extended to assert the T2-BS-3 acceptance row. Phase 3 BACKEND dispatch is the immediate next action.

**Phase 3 SHIPPED 2026-05-24** on branch `dc1-phase-3` (single-PR Option A; 10 commits — 8 implementation + 1 closeout/docs-sweep + 1 V/R/Q-driven cleanup (`b997ce6`, 2026-05-25)). All 14 plan tasks landed: `cleaneddata` package with `CleanedFinancialData` + `FinancialDataView` DTO + `ViewKind` enum + three lazy memoized accessors (`AsReported()` identity copy, `Restated()` sum-of-components + plug + LedgerEntry deltas, `InvestedCapital()` Restated + OverlaySpec entries); LOAD-BEARING C6 invariant honored — `Restated()` reads `e.EquityOffset` directly from the LedgerEntry, never derives it from `DeltaAmount`. `Service.CleanFinancialDataWithViews(ctx, data)` sibling on `DataCleanerService` returns `(*CleaningResult, *cleaneddata.CleanedFinancialData, error)` (additive; `CleanFinancialData` signature unchanged). Q2 resolved: A2 populates `LedgerEntry.TaxShieldDTA = writedownAmount × EffectiveTaxRate` when ETR > 0; pinned by `TestQ2_A2TaxShieldDTA_Populated` (table-driven, 4 ETR scenarios). Q4 resolved: B3 `AIProvenance.PromptHash` + `SourceDocHash` are SHA-256 hex digests computed PRE-API-CALL via new `internal/services/datacleaner/adjustments/hash.go` (canonical prompt hash excludes RequestTimestamp; map-keys sorted for determinism); pinned by `TestQ4_AIProvenance_SHA256_Deterministic`. `ctx context.Context` now first parameter on `Process{Asset,Liability,Earnings}Adjustments`; `applyActiveAdjustments` and `pipeline.go` pass through the cleaner's request-scoped ctx; B3's `captureB3AIProvenance` AI path now respects upstream cancellation. `CurrentSchemaVersions["FinancialData"]` bumped 8 → 9 atomic with the Q2-populating commit (`feedback_schema_version_atomic_bump`); replay testdata `happy/00-manifest.json` synced. **T2-BS-3 acceptance signal LIVE on real fixtures:** `TestLedger_BasketSnapshot_T2BS3_RestatedReconstruction` on the 2026-05-19 baseline — AMD 2023Q2 reconstructs to $9.679B, KO 2023Q2 to $60.912B (parser-stamped TotalLiabilities=0 → Restated reconstruction). All load-bearing invariants stayed GREEN at every commit (`TestDDM_LegacyPath_BitForBit`, `TestOrchestrator_LedgerOrdering`, `TestRecomputeUmbrellas_NoMutation`, `TestDataCleanerRecompute_ShadowMode_TickerBasket` + byte-identical shadow snapshots, `TestLedger_BasketSnapshot_ClusterPrediction`). Translator extraction CLOSED as KEEP per-rule (spec §5.4); Phase 4 deletes alongside dual-write. NON-goals honored: no consumer migration, no B3 routing flip, no dual-write deletion, no CalculationVersion bump (all Phase 4 scope). **Phase 4 is the next gate**: 13-site consumer migration + B3 `data.TotalDebt` dual-write deletion + WACC consumer flip to `InvestedCapital.DebtLikeClaims`. Closeout: `docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-3-closeout.md`.

---

## Context

The financial datacleaner (`internal/services/datacleaner/`) applies a battery of adjuster rules (A1 goodwill exclusion, A2 intangible writedown, A5 inventory writedown, valuation-allowance, etc.) that mutate `entities.FinancialData` in place before the valuation engine consumes it. Three structural issues coexist in the current implementation, exposed by the MXL Q1 2026 case bundled in `artifacts/2026-05-03/MXL/req_9f02dfbc-0993-4508-b2eb-ffff52fd71f6/`:

### B1 — Component / umbrella desync

`assets.go:228-232` reduces `Inventory` and `TotalAssets` together, but **does not propagate** to `CurrentAssets` even though inventory is a current-asset component.

| Field | `10-clean-input.json` | `10-clean-output.json` | Expected after a $34.34M inventory writedown |
|---|---:|---:|---:|
| `inventory` | $85,839,000 | $51,503,400 (−$34.336M) | ✓ |
| `total_assets` | $771,267,000 | $387,402,067 (−$383.86M, sum of all 3 adjusters) | ✓ |
| `current_assets` | $249,450,000 | $249,450,000 (unchanged) | ❌ should be $215,114,400 |

Any consumer that reads `CurrentAssets` post-cleaning sees a stale value. The full list of consumers should be audited; at minimum the new NCAV computation in `internal/services/valuation/graham.go` and the working-capital checks in the existing equity-bridge.

### B2 — `StockholdersEquity` is never mutated by any adjuster

Grep confirms zero `StockholdersEquity` mutations across `internal/services/datacleaner/`:

```
$ grep -rn "StockholdersEquity\s*[-+*/]=\|\.StockholdersEquity\s*=" internal/services/datacleaner/
(no matches)
```

After cleaning, MXL has `total_assets = $387.4M`, `stockholders_equity = $454.2M`, implied `total_liabilities = −$66.8M` — a violation of the accounting identity. This was harmless until now because no downstream math used `stockholders_equity` directly, but every new feature that needs a balanced balance sheet (NCAV, tangible book equity per share, ROE-based screens, distress signals) hits this immediately.

### B3 — Conflation of "valuation overlay" with "as-of restatement"

The deeper issue. Two semantically distinct rule families share the same mutation pipeline:

| Rule type | Examples (current code) | Conceptual meaning | Should restate balance sheet? |
|---|---|---|---|
| **As-of restatement** | A5 inventory obsolescence; A2 intangible impairment; valuation-allowance on DTAs | "This asset is worth less than reported. The balance sheet was misstated." | **Yes** — flows through equity (real economic loss). |
| **Valuation overlay** | A1 goodwill exclusion (Damodaran-style ROIC normalization); capitalised-software exclusion; lease capitalisation | "For invested-capital / ROIC analysis, exclude this. The reported balance sheet itself is correct as filed." | **No** — overlay only, leave underlying data unchanged. |

A1's *wording* in `assets.go:107` is correct: `"goodwill_exclusion: Excluded %.0f goodwill from asset base (%.1f%% of assets)"`. But the *implementation* mutates `data.TotalAssets -= originalGoodwill` and `data.Goodwill = 0.0`, restating the balance sheet. The wording and the code disagree.

For MXL the scale matters: $318.6M of goodwill exclusion (45.1% of pre-clean assets) drives most of the $383.86M total-assets reduction. Treating it as an overlay rather than a restatement closes the bulk of the asymmetry.

---

## Why it matters

- **NCAV ships with derived-fallback warnings until this is fixed.** The Graham-floor spec's TotalLiabilities resolution chain (direct XBRL → derive → unresolved) routes around the cleaner asymmetry by preferring as-reported XBRL. But for any ticker where the SEC parser misses the umbrella tag (uncommon but real for some ADRs / 20-F filers), the derived path will fire and emit a warning. Fixing DC-1 lets NCAV consume cleaned values directly without the workaround.
- **Future features will compound.** Distress screens (Altman-Z, Piotroski-F), ROE / ROA decompositions, P/B ratios, sector-relative book-value comparisons all need `Assets = Liabilities + Equity` to hold. Each one will either (a) work around the cleaner like NCAV does, accreting workarounds; or (b) silently produce wrong numbers.
- **The current data-quality score (90, "A" grade in MXL's case) doesn't catch the asymmetry.** The score evaluates whether rules applied without errors, not whether the resulting balance sheet is internally consistent. Fixing DC-1 also surfaces the need for a balance-sheet-identity check in the cleaner's quality assertions.

---

## Proposed direction (sketch — formal ARCH cycle required before implementation)

Three layers, in dependency order. Each layer alone is insufficient; the combination is the proper fix.

### Layer 1 — Component primitive

Forbid direct mutation of "umbrella" values (`TotalAssets`, `CurrentAssets`, `TotalLiabilities`). Adjusters mutate components only; umbrellas are recomputed:

```go
// internal/services/datacleaner/recompute.go (new)
func recomputeUmbrellas(d *entities.FinancialData) {
    d.CurrentAssets = d.CashAndCashEquivalents +
        d.AccountsReceivable + d.Inventory + d.PrepaidExpenses + d.OtherCurrentAssets
    d.TotalAssets = d.CurrentAssets + d.PPE + d.Goodwill + d.OtherIntangibles +
        d.DeferredTaxAssets + d.OtherNonCurrentAssets
    // (similar for liabilities side)
}
```

Called once at the end of the adjustment pipeline. Adjusters become single-purpose: A5 mutates `Inventory` only; A2 mutates `OtherIntangibles` only; the umbrella propagation is automatic.

**Cost:** moderate. `entities.FinancialData` may need new component fields it doesn't have today (e.g. `OtherCurrentAssets`, `OtherNonCurrentAssets`); SEC parser needs to populate them; every adjuster's existing `data.TotalAssets -=` line is removed.

### Layer 2 — Separate adjuster types: `Restater` vs `Overlay`

Introduce two distinct adjuster interfaces. Restaters apply mutations and stamp `data.AdjustmentLedger` for the equity offset. Overlays produce a parallel view without touching the underlying data.

```go
type Restater interface {
    Apply(d *entities.FinancialData) (delta float64, err error) // mutates components, returns equity-offset amount
}

type Overlay interface {
    ApplyOverlay(d *entities.FinancialData, view *InvestedCapitalView) error // mutates view only
}
```

Existing rules get reclassified:
- **Restaters:** A2 (intangible impairment), A5 (inventory writedown), valuation-allowance on DTAs.
- **Overlays:** A1 (goodwill exclusion for invested capital), capitalised-software exclusion, lease capitalisation for ROIC.

### Layer 3 — Three named views, not one

Replace the current single `*entities.FinancialData` output with a triple:

```go
type CleanedFinancialData struct {
    AsReported        *entities.FinancialData  // direct from SEC parser; never mutated post-fetch
    Restated          *entities.FinancialData  // AsReported + Type-1 adjusters; equity offsets applied
    InvestedCapital   *entities.FinancialData  // Restated + Type-2 overlays; for DCF/ROIC
    Adjustments       []Adjustment             // audit trail (existing)
    Flags             []Flag                   // existing
}
```

Each downstream consumer chooses the view that matches its semantic intent:
- NCAV → `AsReported.CurrentAssets`, `AsReported.TotalLiabilities`
- Tangible book equity per share → `Restated.TangibleAssets`, `Restated.StockholdersEquity`
- DCF / WACC / revenue-multiple → `InvestedCapital.*`
- Per-share metrics that mix views (e.g. `dcf_value_per_share`) read each value from its semantically-correct view

This is the largest change. Affects:
- `entities.CleaningResult` (replaces flat `*FinancialData` output)
- Every downstream consumer in `internal/services/valuation/` (audit needed)
- Persistence layer (`valuation_results` may need view-prefixed columns or a single canonical view per consumer)
- Replay tooling (the bundle artifacts currently snapshot one cleaned view; would snapshot three)

---

## Open questions for the formal ARCH cycle

1. **Ledger-based equity offsets vs. derive-from-deltas?** When a Restater mutates components, do we explicitly track the equity offset in an `AdjustmentLedger` field on `FinancialData`, or recompute equity at the end as `AsReported.StockholdersEquity − sum(restater.deltas)`? Ledger is more transparent; derivation is less code.
2. **Component completeness.** Does the SEC XBRL parser already populate every component the cleaner needs to recompute umbrellas (cash, AR, inventory, prepaid, other-current; PPE, goodwill, intangibles, DTAs, other-non-current)? An audit pass on `internal/infra/gateways/sec/parser.go` is required before the refactor scope is real.
3. **Persistence shape.** The `valuation_results` and `financial_data` tables currently store a single denormalised view. Three views imply either three rows per fetch (with a `view` discriminator), three sets of columns, or canonical-view-per-consumer with the others computed on read. Trade-offs around storage cost vs. replay parity.
4. **Migration / backfill.** Existing cached cleaned data uses the old single-view shape. Cut over with a `cache_version` bump? Or run dual-shape writes for a transition period?
5. **Replay parity.** `internal/observability/replay/` snapshots the cleaning output as `10-clean-output.json`. The new shape needs a backwards-compat read path, or replay's golden bundles will all fail to deserialize.

---

## Suggested next step

Open an ARCH cycle to produce `docs/refactoring/spec/datacleaner-component-primitive-and-parallel-views-spec.md` covering the three layers above with concrete file-by-file deltas, a phased migration plan (Layer 1 → Layer 2 → Layer 3, each independently mergeable), and a regression-test strategy that pins the existing DCF outputs against pre-refactor values for a representative ticker basket (AAPL, MSFT, JNJ, KO, T, F, AMD, MXL, TSM, BABA).

Estimate before formal scoping: 2–3 weeks of focused work, comparable in size to the Phase 2.D replay-tooling refactor.

---

## Acceptance for closing this tracker

- [ ] ARCH spec filed at `docs/refactoring/spec/datacleaner-component-primitive-and-parallel-views-spec.md`.
- [ ] BACKEND implements Layer 1 (component primitive); existing tests stay green; new property-based test asserts `Assets = Liabilities + Equity` post-clean for a randomized fixture set.
- [ ] BACKEND implements Layer 2 (Restater / Overlay split); A1 reclassified as Overlay; A2 + A5 as Restaters with explicit equity offsets.
- [ ] BACKEND implements Layer 3 (three views); downstream consumers migrated one by one; replay golden bundles regenerated.
- [ ] Graham-floor metrics derive `TotalLiabilities` from `AsReported` view, dropping the warn-on-derivation fallback path.
- [ ] CLAUDE.md "Common Gotchas" section updates to reflect the new view shape; the existing single-view note is retired.
