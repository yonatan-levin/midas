# CLAUDE.md - Midas DCF Valuation API

This file provides guidance for AI assistants (Claude, etc.) working on this codebase.

READING AGENTS.md IS MANDATORY.

## Project Overview

**Midas** is a production-grade REST API for equity valuation using Discounted Cash Flow (DCF) analysis. It fetches real-time financial data from SEC EDGAR, market prices from Yahoo Finance/Finzive, and macroeconomic indicators from FRED, then normalizes, cleans, and uses that data to calculate intrinsic value per share.

- **Module**: `github.com/midas/dcf-valuation-api`
- **Go Version**: 1.23+ (toolchain 1.24.4)
- **Current Version**: v0.9.0-rc1 (MVP)

## Build & Run Commands

```bash
# Build
go build ./cmd/server

# Run locally
go run cmd/server/main.go

# Run tests (all)
go test ./...

# Run tests with coverage
go test -cover ./...

# Run a specific package's tests
go test ./internal/services/valuation/...

# Run integration tests only
go test ./internal/integration/...

# Apply database schema + migrations + demo data
go run ./cmd/migrate -db ./data/midas.db

# Seed a demo API key
go run ./cmd/seed-demo-key -db ./data/midas.db

# Hash an API key (utility)
go run ./cmd/hash-key -key <your-key>

# Docker
docker-compose up -d              # Development
docker-compose -f docker-compose.prod.yml up -d  # Production

# Launch staging (scripts)
./scripts/launch_staging.sh       # Linux/macOS
.\scripts\launch_staging.ps1      # Windows

# Contract fuzz testing
./scripts/contract_fuzz.ps1 -DemoKey '<key>' -ApiBase 'http://localhost:8080' -DbPath './data/midas.db'

# Load testing
go run ./scripts/load_tester.go -url http://localhost:8080 -key <API_KEY> -type single -concurrency 20 -duration 60s -rps 20

# Observability lint guard (Phase S) — fails if request-path code uses singleton loggers
# instead of logctx.Or(ctx, ...). Run before committing service/gateway changes.
# Requires ripgrep: `choco install ripgrep` (Windows) / `brew install ripgrep` / `apt-get install ripgrep`.
./scripts/lint-logs.sh           # Linux/macOS
.\scripts\lint-logs.ps1          # Windows

# Prometheus singleton-registerer lint guard (Phase 2.D R3a Stage I.0) — fails if
# replay-package code references prometheus.DefaultRegisterer (must use per-instance
# registries to keep replay binary hermetic under parallel --workers).
./scripts/lint-prometheus-registers.sh   # Linux/macOS
.\scripts\lint-prometheus-registers.ps1  # Windows

# Replay tooling (Phase 2.D — replays captured artifact bundles through current code
# and diffs against saved 17-response.json). All R0–R3 SHIPPED on master.

# Single-bundle replay (default --from=raw)
go run ./cmd/replay artifacts/2026-05-06/MXL/req_c01bec94-9c3c-46f6-afad-9458672c8534/

# Parallel batch replay across a watchlist of bundles (R3a)
go run ./cmd/replay --workers=4 --format=json artifacts/2026-04-25/

# Filter to a specific ticker or recent bundles only (R3a)
go run ./cmd/replay --filter-ticker=AAPL --filter-since=7d artifacts/

# Tunable float tolerances (R3a — defaults: rel 1e-9, abs 1e-12; --float-rel-tol=0
# means "use default", NOT exact-match)
go run ./cmd/replay --float-rel-tol=1e-6 --float-abs-tol=1e-9 artifacts/

# Per-stage diff detail (intermediate-stage drift inspection — R3b). Diffs the bundle's
# saved 10-clean-output.json / 12-growth-curve.json / 13-wacc.json / 15-valuation.json
# against what the engine produces today. --verbose renders per-stage diff sections
# beneath each bundle row.
go run ./cmd/replay --diff-stages --verbose --from=parsed artifacts/2026-05-06/MXL/req_c01bec94-9c3c-46f6-afad-9458672c8534/

# Regenerate JSON contract golden fixtures (R3b Stage M.1) — only run after a
# deliberate change to the JSON output shape. Then `git diff` to verify.
UPDATE_GOLDEN=1 go test -run TestRenderJSON_GoldenFixture ./internal/observability/replay/

# Performance benchmarks (R3b Stage N) — NF2 single-bundle ≤200ms / NF3 100-bundle
# batch ≤30s, both with 3× CI slack. Bench-gated; the synthetic 100-bundle corpus
# generator only fires when -bench is invoked, NOT on default `go test ./...`.
go test -bench=BenchmarkReplay -benchtime=10x ./internal/observability/replay/
```

## Architecture

Clean Architecture (Hexagonal / Ports & Adapters) with dependency injection via `uber/fx`.

```
cmd/                    # Entry points (server, migrate, seed-demo-key, hash-key, apply-schema)
internal/
  api/                  # HTTP layer (Gin router, middleware, handlers)
    v1/handlers/        # Request handlers (fair_value, health, auth, performance)
  config/               # Viper-based configuration loading + XBRL/industry/flag configs
  core/                 # Domain layer (no external dependencies)
    entities/           # Domain models (FinancialData, MarketData, MacroData, etc.)
    ports/              # Interface definitions (gateways, repositories, services)
  di/                   # Dependency injection container (fx modules)
  infra/                # Infrastructure adapters
    database/           # SQL schema
    gateways/           # External API clients
      sec/              # SEC EDGAR API client
      market/           # Yahoo Finance + Finzive market data
      macro/            # FRED macro data
    repositories/       # Data persistence
      sqlite/           # SQLite repository implementations
      cache/            # Redis + in-memory cache implementations
    resilience/         # Circuit breaker, retry policies
  services/             # Business logic services
    auth/               # API key authentication
    datacleaner/        # Financial data normalization pipeline
      adjustments/      # Asset, liability, earnings adjusters
      ai/               # AI-powered footnote analysis (optional)
      flagging/         # Risk flag detection
      industry/         # Industry classification
    datafetcher/        # Multi-source data coordination
    metrics/            # Prometheus metrics collection
    ratelimit/          # Rate limiting
    scheduler/          # Background job scheduler
    growth/             # Forward-looking growth estimation service
    valuation/          # Valuation orchestration engine
      models/           # Industry-specific models (DCF, DDM, FFO, Revenue Multiple)
    watchlist/          # Scheduler watchlist management
    alerting/           # Alert configuration
  observability/        # Cross-cutting logger plumbing (added Phase O of observability upgrade)
    logctx/             # Context-scoped logger inject/extract
    calclog/            # Calculation-stage trace emitter (gated by logging.trace_calculations)
  integration/          # Integration tests
pkg/
  finance/              # Shared financial calculation libraries
    dcf/                # DCF calculation
    growth/             # Growth rate estimation
    wacc/               # WACC calculation
    leases/             # Lease estimation
config/                 # Configuration files (YAML, JSON)
  datacleaner/          # Rules, industry codes, XBRL tag mappings, flag conditions
  alerting/             # Alert rules and notification channels
docs/                   # Documentation (OpenAPI spec, integration plans)
scripts/                # Build, deploy, and test scripts
data/                   # SQLite database files (gitignored)
```

## Key Conventions

### Code Style
- **No globals** - All state managed through DI container
- **Interface-first** - All external dependencies defined as interfaces in `internal/core/ports/`
- **Structured logging** - Use `go.uber.org/zap` exclusively, never `log` or `fmt.Println`
- **Request-path logs via `logctx.From(ctx)`** - Any log line emitted during an HTTP request must go through `internal/observability/logctx.From(ctx)` so it inherits `request_id` (and `user_id`/`key_id` post-auth). Reserve the fx-provided singleton `*zap.Logger` for startup, shutdown, scheduler, and other non-request contexts.
- **Error wrapping** - Use `fmt.Errorf("context: %w", err)` for error chains
- **Context propagation** - All service/repository methods accept `context.Context` as first parameter
- **RFC 7807** - Error responses follow Problem Details format

### Testing
- **TDD mandatory** - Write tests before implementation
- **Table-driven tests** - Use `[]struct{name string; ...}` pattern for test cases
- **Coverage target** - >= 90% for critical finance modules, >= 80% overall
- **Property-based testing** - Use `gopter` for financial calculation correctness
- **Test naming** - `TestServiceName_MethodName_Scenario`
- **Mocks** - Use `testify/mock` for interface mocking
- **Integration tests** - Located in `internal/integration/`, use `testcontainers-go` where needed

### Configuration
- Viper-based: reads from `config/config.yaml`, then env vars (e.g., `DATABASE_DRIVER`)
- Env var mapping: nested keys use `_` separator (`database.driver` -> `DATABASE_DRIVER`)
- Secrets: never committed; use env vars or vault references
- Feature flags: `SCHEDULER_ENABLED`, `DATACLEANER_ENABLE_AI_INTEGRATION`, `ENABLE_SWAGGER`, `ENABLE_PPROF`

### API Design
- All protected endpoints require `X-API-Key` header
- Permission-based access control (`read:fair_value`, `read:health`, `read:metrics`, `manage:keys`, `admin`)
- Rate limiting on all endpoints (API key-based or IP-based fallback)
- Security headers on all responses (HSTS, CSP, X-Frame-Options)

### Database
- SQLite3 (default) or PostgreSQL
- Schema in `internal/infra/database/schema.sql`
- Migrations via `cmd/migrate`
- Repository pattern for all data access

## Important Files

| File | Purpose |
|------|---------|
| `cmd/server/main.go` | Application entry point |
| `internal/di/container.go` | DI container wiring |
| `internal/api/server.go` | HTTP server, routes, middleware |
| `internal/config/config.go` | Configuration structs and loading |
| `internal/core/ports/gateways.go` | External service interfaces |
| `internal/core/ports/repositories.go` | Data storage interfaces |
| `internal/services/valuation/service.go` | DCF valuation orchestration |
| `internal/services/valuation/errors.go` | Sentinel errors (ErrTickerNotFound, ErrInsufficientData, ErrModelNotApplicable) |
| `internal/services/valuation/options.go` | ValuationOptions (override beta/risk-free rate) |
| `internal/services/valuation/params/` | **Single authoritative knob-resolution path** (request-valuation-overrides feature). `params.Overrides` is the domain-layer projection of the `ValuationOverrides` transport DTO. `params.Resolve*` functions implement the config → profile → request-override precedence chain for every valuation knob (beta, risk-free rate, terminal growth, horizon, tax rate, market risk premium, terminal method, growth stage durations, max/min growth). Layer-2 cross-knob invariant checks live here — `params.ParamError` is the typed error the handler maps to 422 `INVALID_OVERRIDE`. The `params` package is the ONLY place knobs are resolved; no handler or valuation model reads config or profile directly. |
| `internal/services/valuation/models/router.go` | Industry-aware model selection (DDM, FFO, Revenue Multiple, DCF) |
| `internal/services/valuation/adr.go` | ADR ticker-to-country mapping + country risk premium lookup |
| `internal/services/valuation/crosscheck.go` | Sanity check: DCF-implied multiples vs sector medians |
| `internal/services/valuation/graham.go` | Graham-school asset-floor diagnostics (current_assets/share, NCAV/share, Graham buy-below floor, discount %). See `docs/refactoring/archive/graham-floor-metrics-spec.md`. |
| `internal/services/valuation/profile/` | **Tier 2 AssumptionProfile package** (Tier 2 COMPLETE 2026-05-21 — Bootstrap + P0a + P0b + P1 + P2 + P3 + P4): full type system — `AssumptionProfile` struct (14 fields) + 21 `Archetype` constants + 3 `Maturity` + `RevenueBaseMethod`/`TerminalMethod`/`DiscountMethod` enums + `ResolvedProfile.IsLegacyMatureLargeBankDDM()` predicate (gates the bit-for-bit DDM legacy path) + `Facts` DTO (pointer-field missing-vs-zero) + `ResolutionTrace`/`AssumptionProfileManifest` + `Registry` interface + `jsonRegistry` impl with SHA-256 `config_hash` + 9 load-time validation invariants + pure 3-stage `Resolve()` algorithm. Wildcard-matched rules report `Source = SourceFallback` per spec §3.3 intent. Import-boundary test enforces zero imports of `models`/`entities`. Profile resolution fires in `service.go::performValuation` before `router.SelectModel`; resolved profile stamped onto `ModelInput.Profile`. **Final config state: `config/assumption_profiles.json` carries 31 profiles + 19 rules** (P0b 2 + P1 6 + P2 6 + P3 6 + P4 11 = 31; `fin_generic` + `insurance` + 5 P1 + 3 P2 + 8 P4 + `fallback_default` = 19). All 8 REIT subsectors have working archetype rules end-to-end (REIT_RESIDENTIAL, REIT_OFFICE, REIT_INDUSTRIAL, REIT_HEALTHCARE, REIT_DATACENTER, REIT_CELLTOWER, REIT_RETAIL, REIT_SPECIALTY). Engine target version after Tier 2 close: `CalculationVersion 4.2` (code bump deferred). |
| `internal/services/valuation/profile/testhelpers/` | Tier 2 cross-package test helpers consumed by P1-P4 worktrees: `BuildMXLModelInput`, `BuildSyntheticAAPLishModelInput`, `BuildSyntheticDataCenterREITInput`, `MustLoadFullFixture`, `LoadGoldenJPMPrimaryValue`, plus `t.Skip` stubs for service-level helpers that P2 wires up. |
| `internal/services/valuation/models/testdata/golden/` | Pre-Tier-2 DDM bit-for-bit golden fixtures (JPM/BAC/WFC × input+output). **DO NOT regenerate** without updating `TestDDM_LegacyPath_BitForBit` and the surrounding governance — these pin the load-bearing mature-large-bank DDM invariant. |
| `internal/services/valuation/models/ddm_bitforbit_test.go` | **LOAD-BEARING regression test** for Tier 2 — uses `math.Float64bits` equality on JPM/BAC/WFC `IntrinsicValuePerShare`/`EquityValue`/`EnterpriseValue` fields. Any Tier-2 commit that fails this test = revert; **never update goldens to make it pass**. |
| `artifacts/tier2-baseline/` | 10-ticker replay baseline captured pre-Tier-2 at master `0324057`. Used by `cmd/replay --diff-stages --from=parsed artifacts/tier2-baseline/` for per-PR regression validation. JPM bundle requires `--allow-schema-drift` due to missing `10-clean-output.json` (cleaner-side bug T2-BS-2). |
| `internal/services/growth/estimator.go` | Multi-stage growth estimation with analyst/historical blending |
| `internal/services/datafetcher/coordinator.go` | Multi-source data fetching |
| `internal/services/datacleaner/service.go` | Financial data normalization |
| `internal/services/datacleaner/industry/classifier.go` | Dual classifier: SIC-based `Classify` (model router) + balance-sheet `ClassifyIndustry` (cleaning rules) |
| `internal/services/datacleaner/industry/classifier_regressions_test.go` | AMD retail-misclassification regression pins (semi basket + sentinel branches) |
| `internal/api/v1/handlers/fair_value.go` | Fair-value handler; owns `FairValueResponse`, `Industry` struct, the canonical SIC→GICS mapping, and `BuildIndustryFromResult` (exported for replay tooling) |
| `cmd/replay/main.go` | Replay CLI (Phase 2.D): re-runs captured artifact bundles through the current valuation engine and diffs against the saved `17-response.json` |
| `internal/observability/replay/` | Replay core: bundle gateways (SEC/Market/Macro/YFinance), `replay.Module` fx composition with hand-picked providers (avoids transitive sqlx/redis), `Replay()` orchestrator, manifest-bound clock binding, schema/git drift detection |
| `internal/infra/gateways/macro/parser.go` | `ParseFREDSeries` pure function (extracted in Phase 2.D R2 Stage A.6) — used by both production `gateway.go` and `BundleMacroGateway` raw-mode |
| `internal/infra/gateways/sec/plugs.go` | **DC-1 Phase 0 SHIPPED 2026-05-16** (merge `1640394`): `computePlugs` pure helper that fills the 4 `Other*` plug fields on `FinancialData` at the end of `parsePeriodData` as residuals (`umbrella − sum(known_components)`, clamped to ≥0 with Debug log on negative residual). Phase 0 invariant: populated but **NO consumer reads them yet** — Phases 1-4 of DC-1 (component primitive shim → Adjuster interface + ledger → CleanedFinancialData view reconstruction → consumer migration) consume them. Spec: `docs/refactoring/spec/datacleaner-component-primitive-and-parallel-views-spec.md`. Plan: `docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-0-implementation-plan.md`. |
| `internal/infra/gateways/market/yfinance_auth.go` | Yahoo Finance cookie+crumb auth manager |
| `internal/infra/database/schema.sql` | Database schema |
| `config/country_risk.json` | Damodaran country risk premiums (30+ countries) |
| `config/industry_multiples.json` | Sector median P/E, EV/EBITDA, EV/Revenue, P/FFO multiples |
| `config/datacleaner/industry_codes.json` | SIC/NAICS/keyword → industry-label mappings; source of truth for what `Classify` emits |
| `docs/openapi.yaml` | OpenAPI 3.0 specification |
| `docs/refactoring/spec/valuation-engine-upgrade-spec.md` | Full upgrade spec: multi-stage growth, industry models, international |
| `docs/refactoring/spec/industry-classification-unification-spec.md` | Planned SIC-only classification refactor (heuristic retirement) |
| `docs/superpowers/specs/2026-04-23-industry-in-response-design.md` | Design spec for the `industry` response field (dual SIC + heuristic with Match flag) |
| `docs/reviewer/` | Tracked follow-up items from code review (D1, D2, B2, W1-W5, S1-S5) |
| `config.env.example` | Environment variable template |

## External Data Sources

| Source | Purpose | Rate Limit |
|--------|---------|------------|
| SEC EDGAR (`data.sec.gov`) | Company financial filings (10-K, 10-Q) | 10 req/sec |
| Yahoo Finance (`query2.finance.yahoo.com`) | Market prices, beta, volume | Cookie+crumb auth, 3 retries |
| Finzive | Fallback market/financial data | Scraper (be polite) |
| FRED (`api.stlouisfed.org`) | Treasury rates, macro indicators | Requires API key |

## Common Gotchas

- Redis is **optional** - the app falls back to in-memory cache if Redis is unavailable
- Windows tests skip some E2E tests (gated by `E2E_LIVE=1`)
- SEC API requires a valid `User-Agent` header with contact email
- SEC EDGAR inconsistently serializes the `cik` field: numeric for some filers (e.g. AAPL: `320193`), zero-padded quoted string for others (e.g. XRTX: `"0001729214"`). Handled by `ports.FlexibleCIK` — do NOT change `SECCompanyFacts.CIK` back to `json.Number` or decode will fail for affected tickers
- **Foreign private issuers (20-F filers) — fully supported as of Phase B of the IFRS-FPI plan** (`docs/refactoring/archive/ifrs-foreign-private-issuer-support-spec.md`). Tickers like TSM (Taiwan Semiconductor), ASML, NVO, AZN, SAP, BABA, BIDU, TM, RIO, BHP, NVS, SHEL, BP all produce real USD per-ADR fair values. The pipeline:
  1. `sec/parser.go` reads any ISO-4217 currency unit and IFRS-full taxonomy concepts; stamps `FinancialData.ReportingCurrency` with the source currency (e.g., TWD).
  2. `valuation/currency.go: convertFinancialsToUSD` FX-converts every monetary field via `MacroDataGateway.GetFXRate` (FRED daily series, falling back to `config/fx_rates.json` when FRED is unavailable). After this step `ReportingCurrency = "USD"`.
  3. `valuation/currency.go: applyADRRatio` divides ordinary-share counts by the configured ratio in `config/adr_ratios.json` (TSM=5, BABA=8, …). Yahoo's reported sharesOutstanding is cross-checked; >10% deviation logs a WARN.
  4. `FairValueResponse` carries `currency: "USD"`, `adr_ratio_applied: <ratio>`, and `current_price: <USD>` for transparency. `current_price` is the live per-share quote captured from Yahoo/Finzive at calculation time, in the same per-share basis as `dcf_value_per_share` (per-ADR for ADRs), so consumers can compute the `(dcf - price) / price` discount without a second quote lookup.

  **The `ports.ErrForeignPrivateIssuer` 422 still ships for**: (a) tickers using genuinely-unmapped taxonomies (JGAAP, K-IFRS, ifrs-smes), (b) currencies with no FRED series AND not present in `config/fx_rates.json`. Both are config-extensible — see `sec/parser.go: findValue` for taxonomy coverage and `internal/infra/gateways/macro/gateway.go: fredSeriesFor` for currency coverage.

  **For new ADR tickers**: verify the depositary ratio against the prospectus before adding to `config/adr_ratios.json` — a wrong ratio silently corrupts per-share values.

  **Distinct from `INSUFFICIENT_DATA`**: clinical-stage biotechs / pre-revenue US companies with `us-gaap` present but no Revenue/OperatingIncome → `HTTP 422 INSUFFICIENT_DATA` (wraps `ports.ErrCompanyFactsNotFound` → `valuation.ErrInsufficientData`, same path as a genuine SEC 404 from `sec/client.go`). The handler **must check FPI first** because it is the more specific case; falling through to INSUFFICIENT_DATA would mask the actionable diagnostic.
- SQLite driver is `github.com/mattn/go-sqlite3` (requires CGO)
- The `config.env.example` file is blocked by a pre-read hook; get config info from `internal/config/config.go` defaults instead
- **Two parallel industry classifiers today** — `Classify(sic, …)` drives the valuation model router and is the canonical label; `ClassifyIndustry(ticker, data)` is a balance-sheet heuristic that drives industry-specific datacleaner rules. They can disagree on the same ticker. The `FairValueResponse.Industry` field surfaces both plus a `match` flag. Do NOT refactor `getIndustryCode` in `datacleaner/service.go` to prefer SIC — that's tracked as the unification refactor in `docs/refactoring/spec/industry-classification-unification-spec.md` and is out of scope for incidental changes
- **`sicToGICS` map in `fair_value.go` keys MUST match `config/datacleaner/industry_codes.json`** `code` fields one-to-one. The classifier emits labels like `FIN` (not `FINL`), plus sub-industry codes `TECH_SAAS`, `HEALTH_BIOTECH`, `FIN_BANK`, etc., and REIT subsector codes in `REIT_*` prefixed form (`REIT_DATACENTER`, `REIT_INDUSTRIAL`, `REIT_RETAIL`, `REIT_HEALTHCARE`, `REIT_RESIDENTIAL`, `REIT_OFFICE`, `REIT_CELLTOWER`, `REIT_SPECIALTY`) per T2-P4-W1 reconciliation (merge `be92a79` 2026-05-19). A mismatch silently demotes entire sectors to `match: false`. Add new top-level labels to both the map and `matchSICToGICS`'s normalization logic simultaneously. REIT subsectors get full-code exact-match entries in `sicToGICS` because `"REIT"` itself is NOT a key — parent-strip fallback can't catch them, so each subsector needs its own explicit entry
- **Replay tooling (Phase 2.D — ALL R0–R3 SHIPPED, COMPLETE as of merge `0741958` 2026-05-09) is hermetic by construction** — `cmd/replay` and `internal/observability/replay/` MUST NOT touch the production database, Redis cache, metrics shipper, scheduler, or external APIs. The `replay.Module` hand-picks `fx.Provide` lines rather than wrapping `di.CoreModule` precisely because CoreModule transitively pulls `*sqlx.DB` and `*redis.Client` constructors which would side-effect even when downstream consumers are decorated away. **Bundle gateways MUST return `replay.ErrBundleMissingPayload` (NOT panic)** on missing files because `internal/services/datafetcher/coordinator.go:181-196` runs gateway calls inside parallel goroutines under `sync.WaitGroup` — a child-goroutine panic would not be recovered by the replay binary. Auth/Watchlist stubs DO panic (different layer; not on the goroutine path). The `cmd/server` ↔ `replay` import-boundary CI guard at `cmd/server/import_boundary_test.go` (R3a Stage O.13) keeps Stage O.6's `init()` reflection panic confined to the replay binary. **Stage K's `--diff-stages` reads bundle JSON files via `os.ReadFile` directly** (rather than re-deriving from entities) so future struct-shape evolution doesn't break diff path. The "current" snapshot for diff comparison uses an ephemeral `os.MkdirTemp` bundle with `defer os.RemoveAll`; never persists (preserves D7 "no bundles of bundles" invariant). When adding a new replay surface, preserve all three invariants (F11 hermeticity, ErrBundleMissingPayload-not-panic, ephemeral-snapshot-only)
- **Graham-floor diagnostic fields (`current_assets_per_share`, `ncav_per_share`, `graham_floor_per_share`, `graham_discount_pct`)** are computed in `internal/services/valuation/graham.go` and stamped onto `ValuationResult` from BOTH the DCF path and the alt-model path in `service.go`. All four are **omitted from the JSON response** when `TotalLiabilities` cannot be resolved (see `resolveTotalLiabilities` fallback chain) — a warning string `"graham_floor: insufficient balance-sheet data..."` or `"graham_floor: derived total_liabilities from balance-sheet identity..."` is appended to `result.Warnings` instead. `graham_discount_pct` uses `*float64 + omitempty` deliberately: nil distinguishes "floor==0, ratio undefined" from `&0.0` (price exactly equals the floor). The derivation fallback (`TotalAssets − StockholdersEquity`) emits a WARN log naming the ticker so operators can correlate against the cleaner asymmetry tracked in `docs/reviewer/DC-1-datacleaner-component-primitive-and-parallel-views.md`. Do NOT add a config flag to suppress these warnings — they are a load-bearing data-quality signal
- **`tangible_value_per_share` denominator changed from market-basic to diluted shares in v0.10.0 (Graham floor metrics PR #2).** Cached pre-v0.10.0 values may be ~2-5% higher; expect drift on first recompute. Priority chain mirrors DCF: diluted → market.basic → financial.basic. See `calculateTangibleValuePerShare` in `internal/services/valuation/service.go` and the regression pin in `service_test.go: TestService_calculateTangibleValuePerShare_DilutedDenominator`
- **DC-1 datacleaner refactor — Phase 0 SHIPPED 2026-05-16, merge `1640394`.** The 4 plug fields on `FinancialData` (`OtherCurrentAssets`, `OtherNonCurrentAssets`, `OtherCurrentLiabilities`, `OtherNonCurrentLiabilities`) are populated by the SEC parser via `computePlugs` at `internal/infra/gateways/sec/plugs.go` but **no consumer reads them yet** as of Phase 0. Zero behavior change in DCF / WACC / Graham / EV-bridge outputs — empirically replay-verified on AAPL + MSFT. **Important corollary**: today's parser only populates the umbrella `OperatingLeaseLiability` (not the `*.Current` / `*.Noncurrent` split fields — they're fallbacks for the umbrella), so in production `OtherCurrentLiabilities` absorbs the entire CurrentLiabilities umbrella and `OtherNonCurrentLiabilities` absorbs everything except `TotalDebt`. Lease-split decomposition is deferred to Phase 1+. Math invariants in the spec hold by construction regardless. The Graham-floor diagnostic (above) cites DC-1 as the cleaner-asymmetry tracker; that asymmetry is what Phases 1-4 close. Phases 2-4 (Adjuster interface + AdjustmentLedger, CleanedFinancialData with `AsReported`/`Restated`/`InvestedCapital` views, consumer migration) follow per `docs/refactoring/spec/datacleaner-component-primitive-and-parallel-views-spec.md`.
- **Tier 2 archetype rules use `(industry_prefix, priority)` matching; for FIN family, currently only `fin_generic` (FIN prefix) + `insurance` (FIN_INSURANCE prefix) fire** because today's classifier emits unified `FIN_BANK` (no large/small split — the heuristic for bank size bucketing isn't implemented). JPM/BAC/WFC route via `fin_generic` → `mature_large_bank` archetype, which is exactly what preserves the JPM bit-for-bit DDM invariant. A future bank-bucketing heuristic that emits distinct `FIN_LARGE_BANK` / `FIN_SMALL_BANK` codes would require: (a) the classifier change itself, (b) new `fin_large_bank` / `fin_small_bank` archetype rules with those prefixes, (c) a careful re-evaluation of the JPM bit-for-bit invariant (since JPM might be re-routed to a non-legacy archetype). See the archived discussion in `docs/reviewer/T2-P4-W1-classifier-prefix-mismatch.md` — the dead `FIN_SMALL_BANK` / `FIN_LARGE_BANK` rules were deleted in P3 defect-fixup `5a72208` precisely because they would never have fired against today's classifier emission.
- **DC-1 datacleaner refactor — Phase 1 SHIPPED 2026-05-19, merge `2d916a7`.** `internal/services/datacleaner/recompute.go::recomputeUmbrellas` runs at the end of the cleaner pipeline (between `createRiskWarningFlags` and `calculateQualityScore`) as a shadow-mode observer — emits WARN lines tagged `"phase":"DC-1-P1-shadow"` for every umbrella divergence, but does NOT mutate `*FinancialData` (the load-bearing invariant is pinned by `TestRecomputeUmbrellas_NoMutation`'s `reflect.DeepEqual` snapshot). Phase 2's `Adjuster` interface refactor consumes the divergence enumeration captured in `internal/integration/testdata/recompute-shadow/<TICKER>.json` — see the shadow-analysis report at `docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-1-shadow-analysis.md` for the 7-cluster Phase 2 punch list. To grep production logs: `rg '"phase":"DC-1-P1-shadow"'`. To re-derive the shadow analysis locally: `go test ./internal/integration/... -run TestDataCleanerRecompute_ShadowMode_TickerBasket -count=1`.
- **Tier 2 bit-for-bit DDM invariant is LOAD-BEARING.** `TestDDM_LegacyPath_BitForBit` (in `internal/services/valuation/models/ddm_bitforbit_test.go`) asserts `math.Float64bits` equality on JPM/BAC/WFC DDM outputs against pre-Tier-2 goldens at `testdata/golden/{jpm,bac,wfc}_ddm_pre_tier2_{input,output}.json`. **Any change to mature-large-bank DDM math that fails this test must be REVERTED — do NOT update the goldens to make it pass.** The fixtures use patched DPS values (JPM=$4.80, BAC=$1.00, WFC=$1.40) from public-record FY2024 data because the production cleaner emits `DividendsPerShare=0` for FIN-prefix tickers (pre-existing bug T2-BS-1). Regenerating goldens via `go test -tags goldencapture` ONLY regenerates outputs from existing inputs — inputs themselves must NOT be re-derived from a live bundle (would produce DPS=0 and silently break the invariant). The `artifacts/tier2-baseline/` directory contains the 10-ticker replay baseline used by `cmd/replay --diff-stages` for cross-Tier-2-phase regression validation. **Tier 2 P3 (VAL-2 DDM multi-stage) SHIPPED 2026-05-21** (merge `59c0fdc`): the JPM bit-for-bit invariant was preserved via **PATH DUPLICATION** — `calculateLegacyGordon` is a byte-identical lift of the pre-Tier-2 `Calculate` body; the new `Calculate` is now a dispatcher gating on `Profile.IsLegacyMatureLargeBankDDM()` → legacy Gordon path, OR `calculateMultiStage` sibling for the new multi-stage path. Multi-stage activates on `DividendForecastHorizon > 0` with populated `PayoutPath`; cost-of-equity discount NOT WACC. Do NOT refactor the legacy/multi-stage dispatcher into a unified path without re-proving the bit-for-bit invariant — the duplication is intentional.
- **T2-P4-W1 classifier prefix reconciliation SHIPPED 2026-05-19** (merge `be92a79`); engine + config defects closed by P3 + P4 defect-fixup commits 2026-05-21 (tracker status now MOSTLY RESOLVED — 4 defects closed, 2 acceptance rows deferred to a Tier 2 Closeout validation pass). Classifier emits `REIT_*` prefixed subsector codes (REIT_DATACENTER, REIT_INDUSTRIAL, REIT_RETAIL, REIT_HEALTHCARE, REIT_RESIDENTIAL, REIT_OFFICE, REIT_CELLTOWER, REIT_SPECIALTY). Downstream consumers updated atomically: `config/industry_multiples.json` keys (v1.3.0), `models/router.go::reitIndustrySet` + defensive `strings.HasPrefix("REIT_")` fallback in `isREITIndustry`, FFO subsector tables (longest-prefix-match unchanged), `handlers/fair_value.go::sicToGICS` (full-code exact-match — `"REIT"` is not a key). FIN side: classifier emits `FIN_INSURANCE` + `FIN_BANK` (no large/small split); `FIN_BANK` matches existing `fin_generic` (FIN prefix) → `mature_large_bank` archetype → preserves JPM bit-for-bit. P3 defect-fixup `5a72208` deleted dead `fin_small_bank` + `fin_large_bank` rules (no classifier emission ever fed them). P4 defect-fixup `b8853c7` renamed `reit_commercial` industry_prefix to `REIT_OFFICE` + added missing `reit_specialty` rule. Tracker `docs/reviewer/T2-P4-W1-classifier-prefix-mismatch.md` stays OPEN until a future Closeout validation pass satisfies the 2 deferred acceptance rows (live API regression on EQIX+PLD + replay regression on `artifacts/tier2-baseline/` — both need running the live engine end-to-end; deferred from this docs-only Closeout sweep).
- **`revenue_multiple` model uses TTM revenue, not `latest.Revenue`** (RM-1 SHIPPED via merge `cfdf7b4`, doc closeout 2026-05-23). `internal/services/valuation/models/revenue_multiple.go::Calculate` calls `entities.HistoricalFinancialData.TrailingTwelveMonthsRevenue()` (defined at `internal/core/entities/financial_data.go:378`). The helper walks a fixed fallback chain — `TTM_PRIOR_BRIDGE → TTM_4Q → ANNUAL_FY → ANNUALIZED_QUARTER → INSUFFICIENT_HISTORY` — and surfaces both the path source and a lossy-path warning on `result.Warnings` (`"revenue_base: source=<TAG> revenue=$<X>"` is always emitted; `"revenue_base: annualized single-quarter revenue (4× extrapolation, ignores seasonality)"` and similar warnings fire only on the lossy paths). `TTM_PRIOR_BRIDGE` is intentionally first so partial-year IPO shapes surface as `TTM_PRIOR_BRIDGE` rather than being silently absorbed into `TTM_4Q` (V/R/Q follow-up `9da6c68`). Source strings are part of the public contract — replay tooling and dashboards key off them; do NOT rename without coordinating with consumers. T7 stale-data check (>=18 months) lives at the consumer in `revenue_multiple.go` via the `monthsSince` helper, NOT in the entity (RM-1.A: keeps the entity package clock-free for replay determinism; clock seam is `ModelInput.Now` plumbed from `*Service.clock`).
- **`scheduler.Service.Start(ctx)` launches a detached supervisor goroutine; tests MUST register `t.Cleanup(func() { cancel(); svc.Stop() })`** (SCHED-1 fix, 2026-05-22). `Start()` captures `s.logger` (typically `zaptest.NewLogger(t)` in tests) and hands it to a background goroutine that logs on tick + job completion + shutdown. Without an explicit drain, the goroutine can outlive `*testing.T` and panic on the next package's test with "Log in goroutine after <Test> has completed". `scheduler.Service.Stop()` is a side-effect-free drain — it blocks until the supervisor + all in-flight job goroutines have exited; it does NOT cancel the context (the caller's `cancel()` does). Production callers (fx `OnStart` in `internal/di/container.go`) don't need to call `Stop()` because the process exits when the root context is cancelled. The internal channel is allocated under `sync.Once` so repeated `Start()` calls don't leak supervisors. Pattern: `ctx, cancel := context.WithTimeout(...); t.Cleanup(func() { cancel(); svc.Stop() }); svc.Start(ctx)`.
- **DC-1 Phase 2 SHIPPED 2026-05-23 (full 4-PR stack merged from `dc1-phase-2-pr-4`).** All 17 cleaner-side adjusters now implement the `Adjuster` interface natively (6 A-rules in PR-2, 7 C-rules in PR-3, 3 B-rules + the PR-1 shim deletion in PR-4); the PR-1 orchestrator shim and both shim helpers (`shimLedgerEntriesFromLegacy` + `shimLedgerEntriesFromLegacyExcluding`) are FULLY DELETED. **Four PRs in stack order:** PR-1 (tip `39cf0fa`) — `Adjuster` interface + `LedgerEntry`/`OverlaySpec`/`AdjustmentLedger`/`AmountSemantics`/`AIProvenance` entities at `internal/core/entities/adjustment_ledger.go`; `AdjustmentLedger []LedgerEntry` + `Overlays []OverlaySpec` fields on `entities.FinancialData`; scaffolding shim with 3 deletion-order branches; `recomputeUmbrellas` WARN line carries `recent_adjusters: []string` (Q1 SHIPPED). PR-2 (tip `2e8f83b`) — A1 OverlayEmitter, A2/A4 Restater, A5 Restater+TaxShieldDTA, 2 flag-only review FlagEmitters; asset-side shim deleted (`2c132aa`); `SchemaVersion` 7→8 atomic with Task 2.1; A-FY-NULL HIGH-confidence NOT-A-BUG (Q3 SHIPPED via tracker at `docs/reviewer/DC-1-FY-enable-predicate-investigation.md`). PR-3 (tip `207f41a`) — C1/C2/C3/C5 Restaters, C6 Restater with **LOAD-BEARING `EquityOffset=0` special case** (capitalized interest reclassifies between income-statement line items, MUST NOT flow to retained earnings — Phase 3's `Restated()` accessor must respect this), C4/C7 FlagEmitters (C4 plan-vs-code disagreement resolved by trust-the-code precedent — legacy code at `ProcessStockCompensationAdjustment` is Reclassify-only, not Restater); earnings-side shim deleted (`4af3c33`). PR-4 (tip `ed1dadd`) — B1 (`613773a`) + B2 (`e328226`) OverlayEmitters with `Field:"TotalDebt"`; B3 (`5c4e88d`) OverlayEmitter with **`Field:"DebtLikeClaims"`** (Phase 4 routing intent — today's Phase 2 dual-write still mutates `data.TotalDebt` for B3) + best-effort `AIProvenance` (`ModelName: b3AIModelName`; `PromptHash`/`SourceDocHash`/`ExtractedSpan` empty per Q4 deferral); orchestrator `data.TotalDebt += result.Amount` dual-write absorbed into `ProcessLiabilityAdjustments`' per-rule switch arms (`37f7400`, Option α — keep wrapper, move mutation into wrapper); liability-side shim branch + both shim helpers deleted (`5eaadd3`); basket integration test `TestLedger_BasketSnapshot_ClusterPrediction` at `internal/integration/datacleaner_ledger_basket_test.go` (Task 4.6); Phase 2 closeout docs sweep (Task 4.7); Phase 3 spec + implementation plan authored (Task 4.7+ `ed1dadd`). **Canonical pattern locked across all 17 adjusters:** mutation-FREE `Apply*` methods on `AssetAdjuster`/`LiabilityAdjuster`/`EarningsAdjuster` (read `working`, return `AdjusterOutput`); the dispatcher `Process*Adjustments` switch owns the dual-write (capture original* → call `Apply*` → translate to legacy `*AdjustmentResult` via per-rule translator → mutate `data.X` → drain `AdjusterOutput.LedgerEntries`/`Overlays`/`Flags` into result's `Native*` slices + add the `RuleID` to `NativelyEmittedRuleIDs`). The cleaner orchestrator drains the per-category `NativeLedgerEntries`/`NativeOverlays` directly onto `data.AdjustmentLedger`/`data.Overlays`; native-drain uses `NativelyEmittedRuleIDs` as the exclusion set. **Four role flavors canonized across all 3 categories:** **OverlayEmitter** (A1, B1, B2, B3 — empty `Component`/`DeltaAmount`/`EquityOffset` on LedgerEntry; monetary delta lives on `OverlaySpec.Amount`); **Restater** (A2, A4, C1, C2, C3, C5, C6 — `LedgerEntry{Component:"X", DeltaAmount:..., EquityOffset:DeltaAmount}` with the C6 `EquityOffset=0` exception); **Restater + TaxShieldDTA** (A5 only — populates `TaxShieldDTA = writedown × working.EffectiveTaxRate` when EffectiveTaxRate > 0); **FlagEmitter convention** (`Fired:false` LedgerEntry with non-empty `AdjusterOutput.Flags` as the firing signal — C4, C7, plus 2 PR-2 reviews). **Q-disposition status:** Q1 SHIPPED in PR-1 (`recompute` WARN `recent_adjusters` enrichment); Q2 DEFERRED to Phase 3 (A2 `TaxShieldDTA` actual population — preserved A2 dual-write bit-for-bit contract; pinned by `TestA2IntangibleAdjuster_Adjuster_Interface_Contract/fired_path_TaxShieldDTA_stays_zero_per_Q2_deferral`); Q3 SHIPPED in PR-2 Task 2.7 (A-FY-NULL tracker); Q4 DEFERRED to Phase 3 (`AIProvenance` SHA-256 hash field computation — zero hashes ship with TODO; today's `ai.AnalyzeFootnote` call site does not compute them). **T2-BS-3 disposition (the AMD/KO `TotalLiabilities==0` parser dropout):** Option B (carve-out) chosen by Phase 2 ARCH on 2026-05-19. Parser fix DEFERRED; `AsReported.TotalLiabilities` stays at 0 for AMD/KO. Phase 3's `Restated` view reconstruction (from `sum(components) + plug`) will produce a non-zero value, correctly surfacing the components-derived liability total. Tracker `docs/reviewer/T2-BS-3-parser-totalliabilities-zero-amd-ko.md` stays OPEN. **`CurrentSchemaVersions["FinancialData"]` at 8** (atomic bump landed in PR-2 Task 2.1 — PR-3 and PR-4 reuse the same structural envelope). **Substantive accuracy correction now visible:** B3's `OverlaySpec.Field:"DebtLikeClaims"` records Phase 4's routing intent (Damodaran convention — contingent liabilities feed `InvestedCapital.WACCInputs.DebtLikeClaims`, NOT `Restated.TotalDebt`). Today's Phase 2 dual-write still mutates `data.TotalDebt` for B3 (legacy preservation); Phase 4's consumer migration flips the read site to consume `Overlays[Field:"DebtLikeClaims"]` and deletes the `data.TotalDebt` mutation. **Load-bearing invariants stayed GREEN across all 4 PRs:** `TestDDM_LegacyPath_BitForBit` (JPM/BAC/WFC — non-trivially preserved in PR-4 because B-rules touch `TotalDebt`; pinned by per-PR-4-commit replay validation), `TestRecomputeUmbrellas_NoMutation`, `TestOrchestrator_LedgerOrdering` (asset → liability → earnings partition), `TestDataCleanerRecompute_ShadowMode_TickerBasket` + shadow-snapshot byte-identity, `TestLedger_BasketSnapshot_ClusterPrediction` (new in Task 4.6). **Phase 3 spec + implementer plan AUTHORED 2026-05-23 — view reconstruction (`CleanedFinancialData`/`AsReported`/`Restated`/`InvestedCapital` accessors), Q2 + Q4 resolutions, ctx threading.** Spec: `docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-3-spec.md`; plan: `docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-3-implementation-plan.md`. Phase 4 (13-site consumer migration + B3 routing flip + dual-write deletion) follows. Plan: `docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-2-implementation-plan.md`. Closeout: `docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-2-closeout.md`.
- **DC-1 Phase 3 SHIPPED 2026-05-24 (single-PR `dc1-phase-3`, Option A).** Introduces `internal/services/datacleaner/cleaneddata` package with `CleanedFinancialData` + `FinancialDataView` DTO + `ViewKind` enum + three lazy memoized accessors: `AsReported()` (identity copy, T2-BS-3 carve-out preserves parser-stamped TotalLiabilities=0), `Restated()` (sum-of-components + plug recompute; applies fired Restater-role `LedgerEntry.DeltaAmount` per Component + `EquityOffset` + `TaxShieldDTA`), and `InvestedCapital()` (Restated + OverlaySpec entries — B1/B2 TotalDebt overlays + B3 DebtLikeClaims overlay all accumulate into `DebtLikeClaims`; A1 goodwill overlay subtracts from TotalAssets + flushes Goodwill to 0 per Damodaran). **LOAD-BEARING C6 invariant respected:** Restated() reads `e.EquityOffset` directly from the LedgerEntry — NEVER derives it from `DeltaAmount` — so C6's capitalized-interest reclassification (DeltaAmount≠0, EquityOffset=0 by design) stays out of retained equity. Pinned by `TestCleanedFinancialData_Restated_C6EquityOffsetZero`. Sibling `Service.CleanFinancialDataWithViews(ctx, data)` returns `(*CleaningResult, *CleanedFinancialData, error)` — ADDITIVE to `CleanFinancialData(ctx, data)` (which stays unchanged). Phase 4 consumers grep for `CleanFinancialDataWithViews` to enumerate migration progress. **Q2 SHIPPED:** A2 `LedgerEntry.TaxShieldDTA = writedownAmount × working.EffectiveTaxRate` when ETR > 0, mirroring A5's Phase 2 pattern. Pinned by `TestQ2_A2TaxShieldDTA_Populated` (table-driven, 4 ETR scenarios) + replaces the Phase 2 `fired_path_TaxShieldDTA_stays_zero_per_Q2_deferral` subtest. **Q4 SHIPPED:** B3 `AIProvenance.PromptHash` + `SourceDocHash` are SHA-256 hex digests computed PRE-API-CALL via `sha256Hex` + `sha256HexPromptCanonical` at `internal/services/datacleaner/adjustments/hash.go`. The canonical-prompt hash excludes `RequestTimestamp` (wall-clock would force non-deterministic hashes) and sorts Context map keys before JSON serialization. Pinned by `TestQ4_AIProvenance_SHA256_Deterministic` (4 assertions: 64-char hex shape; identical-input determinism; SourceDocHash byte-equal to SHA-256(footnoteText); footnote-text sensitivity). **ctx threading SHIPPED:** `ProcessAssetAdjustments` / `ProcessLiabilityAdjustments` / `ProcessEarningsAdjustments` all gain `ctx context.Context` as the first parameter; service.go::applyActiveAdjustments and pipeline.go pass through the cleaner's request-scoped ctx. The internal `var applyCtx context.Context` nil-shim is replaced with `applyCtx := ctx`. B3's `captureB3AIProvenance` now receives the real ctx so AI-path cancellation propagates upstream. 5 test files with a local `context := &entities.CleaningContext{...}` variable shadow gained a `gocontext "context"` import alias and use `gocontext.Background()` at the new call sites. **`CurrentSchemaVersions["FinancialData"]` bumped 8 → 9** atomic with the Q2 commit per `feedback_schema_version_atomic_bump`. **T2-BS-3 acceptance signal LIVE:** `TestLedger_BasketSnapshot_T2BS3_RestatedReconstruction` (`internal/integration/datacleaner_ledger_basket_test.go`) confirms `AsReported.TotalLiabilities==0` + `Restated.TotalLiabilities>0` on real captured 2026-05-19 baseline bundles — AMD 2023Q2 reconstructs to $9.679B; KO 2023Q2 to $60.912B. This is the spec §10 item 4 gate Phase 4 ARCH needed before consumer-migration dispatch. **All load-bearing invariants stayed GREEN at every commit:** `TestDDM_LegacyPath_BitForBit` (JPM/BAC/WFC bit-for-bit), `TestRecomputeUmbrellas_NoMutation`, `TestOrchestrator_LedgerOrdering` (asset → liability → earnings partition), `TestDataCleanerRecompute_ShadowMode_TickerBasket` + shadow-snapshot byte-identity (`git diff --quiet internal/integration/testdata/recompute-shadow/` exits 0 at every commit), `TestLedger_BasketSnapshot_ClusterPrediction` (10/10 basket tickers). **Translator-extraction question CLOSED as KEEP per-rule** (spec §5.4) — Phase 4 deletes translators alongside the dual-write; no extraction in Phase 3 or Phase 4. **NON-goals honored (NO consumer migration / NO B3 routing flip / NO dual-write deletion / NO CalculationVersion bump — all Phase 4 scope).** Spec: `docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-3-spec.md`. Plan: `docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-3-implementation-plan.md`. Closeout: `docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-3-closeout.md`. **MERGED to master 2026-05-25 as `46e84b1`** (the Phase 3 followup at `3490227` lands V/R/Q-driven correctness fixes for HIGH-1 view-seed double-count + HIGH-2/3 B3 single-AI-call + ctx; see the followup CLAUDE.md bullet below for details).
- **DC-1 Phase 3 followup SHIPPED 2026-05-25 (single-PR `dc1-phase-3-followup`, 10 commits).** Closes 9 cross-model review findings (gpt-5.5 via zen-mcp) against the Phase 3 merge. **HIGH-1 (F.1):** `Restated()` view-seed double-count fixed via pre-clean snapshot inside `CleanFinancialDataWithViews` — `cleaneddata.New(asReported, restated)` signature now takes BOTH the pre-clean snapshot AND the post-clean entity; the reducer drives equity offsets + DTA shields ONLY (LedgerEntry.DeltaAmount is no longer re-applied because the dispatcher dual-write has already restated component fields). `applyLedgerEntryToView` helper deleted. The pre-fix code double-counted every Restater fire (A2 OtherIntangibles, A4 Inventory, A5 Inventory + TaxShieldDTA, C1/C2/C3/C5 NormalizedOperatingIncome, C6 InterestExpense). **HIGH-2 + HIGH-3 (F.2):** B3 collapsed to single `analyzeContingentLiabilityWithAI(ctx, data, cleaningCtx, timestamp)` call returning `(probability, *AIProvenance, metadata, error)`; `captureB3AIProvenance` DELETED. The recorded overlay.Amount and overlay.AIProvenance.Probability now derive from the SAME AI response (audit integrity restored); the ctx threading also fixes the amount-path's previously-hardcoded `context.Background()` so request-scoped cancellation propagates. `ApplyB3Contingent` pre-computes the AI result and injects it via a new `preComputedAIResult` parameter into the shared `processContingentLiabilityAdjustment` helper; the legacy public `ProcessContingentLiabilityAdjustment(ctx, ...)` signature gains ctx and runs the AI call once internally on the direct-call path. **MEDIUM-1 (F.3):** B1 lease PV ctx threading — `ProcessOperatingLeaseAdjustment(ctx, ...)` + `ApplyB1OperatingLeases(ctx, ...)` forward ctx to `leaseCalculator.CalculatePresentValue`; the hardcoded `ctx := context.Background() // TODO` line is deleted. **MEDIUM-2 (F.4):** Phase 3 spec §5.2 amended — `PromptHash` documented as **canonical-request fingerprint** (timestamp-stripped serialization of `ai.FootnoteAnalysisRequest` fields, sorted Context keys), NOT literal LLM prompt-as-sent. The AI client at this layer does not expose a rendered-prompt string; the canonical-request hash satisfies the underlying intent. Prompt-template drift is captured by `AIProvenance.ModelName`. No code change for F.4 — `sha256HexPromptCanonical`'s behavior is unchanged; the amendment aligns spec wording with implementation. **LOW-1 (F.5):** `hash.go::sha256HexPromptCanonical` hardened — inner `json.Marshal` errors emit `<unsupported:%T>` tag (was: silent empty-string collision); outer Marshal gets panic-on-error guard. **LOW-2 (F.6):** godoc warning on `cleaneddata.CleanedFinancialData` — NOT goroutine-safe (accessor cache fields are lazily populated without locking); future parallel-read consumers would need a `sync.Once` retrofit. **LOW-3 (F.7):** `TestIdentityCopy_CoversEveryViewField` reflection-based field-coverage test enumerates `FinancialDataView` fields, builds a synthetic entity with distinct sentinel values, and asserts every non-exempt view field is non-zero post-`identityCopy`. Guards against silent zero-value bugs when adding new fields. **LOW-4 (F.8):** `TODO(phase-5)` on `Raw()` + new "Phase 5 — Cleanup: delete Raw() migration escape hatch" row in the parent spec's "Phasing & implementation sequence" table. **NEW load-bearing pin: `TestCleanFinancialDataWithViews_Restated_NoDoubleCount_OnRestaterFire`** — exercises the full pipeline on an A2-firing fixture; without it, HIGH-1 can silently re-regress. Sibling pin `OnEarningsFire` covers C1 restructuring. **NON-goals preserved (Phase 4 scope): NO consumer migration / NO B3 routing flip / NO dispatcher dual-write deletion / NO `CalculationVersion` bump (4.2) / NO `SchemaVersion` bump (9) / NO changes to `recompute.go` / NO DDM golden regeneration.** All load-bearing invariants stayed GREEN at every commit: `TestDDM_LegacyPath_BitForBit`, `TestRecomputeUmbrellas_NoMutation`, `TestOrchestrator_LedgerOrdering`, `TestLedger_BasketSnapshot_ClusterPrediction`, `TestLedger_BasketSnapshot_T2BS3_RestatedReconstruction` (AMD/KO still reconstruct), shadow-snapshot byte-identity. Spec: `docs/refactoring/archive/dc1-phase-3-followup-spec.md`. Plan: `docs/refactoring/archive/dc1-phase-3-followup-implementation-plan.md`. Closeout: `docs/refactoring/archive/dc1-phase-3-followup-closeout.md`. **MERGED to master 2026-05-26 as `3490227`** (12 commits on the branch — ARCH + F.1-F.10 + F.11 REVIEWER-nits cleanup). Phase 4 ARCH spec + implementer plan filed at `docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-4-spec.md` + `docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-4-implementation-plan.md`.
- **DC-1 Phase 4 MERGED to master 2026-05-27 as `ce94f70` (8 commits — 5 clusters + 2 review-fix followups + docs).** Consumer migration + B3 routing flip + dispatcher dual-write deletion + `CalculationVersion` 4.2 → 4.3. **Reviewed across 4 rounds:** `/execute` B-V-R-Q on the 6-commit base → REJECT(NWC HIGH); `/code-review` holistic high-effort → APPROVE_WITH_NITS; zen-mcp **gpt-5.5** cross-model → revenue_multiple HIGH (+ 1 finding refuted as intended Class-II drift); `/execute` B-V-R-Q on the fix → VERIFIED/APPROVE/PASS. **Two correctness regressions found + fixed:** `e521c53` — NWC `calculateNetWorkingCapitalChange` flipped `Restated()` → `AsReported()` (`CurrentAssets`/`CurrentLiabilities` are RECOMPUTED-umbrella fields; the Phase-0 plug under-reconstructs on AMD-class tickers, drifting FCF even with zero Restaters firing); and `2ea9978` — `revenue_multiple` EV→Equity bridge now subtracts `InvestedCapital().DebtLikeClaims` (was silently dropping B1/B2/B3 claims after the dual-write deletion; FFO unaffected — multiple-derived equity; DDM=0). **Phase 5 spec + implementer plan AUTHORED** on `dc1-phase-5-prep`: `docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-5-spec.md` + `docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-5-implementation-plan.md` (DDM view migration + DDM DebtLikeClaims correction + translator retirement + `Raw()` deletion; CalcVersion → 4.4). **Commit ladder:** C-1 `f65604a` (plumbing — thread `*cleaneddata.CleanedFinancialData` through `performValuation`/`performAlternativeValuation`); C-2 `4f8a06c` (ROIC + NWC + `effectiveOI` + Graham migrate; delete A2/A4/A5 dual-writes); C-3 `9bc885e` (DCF FCF + cross-check + router OI + alt-model OI migrate; delete C1/C2/C3/C5/C6 dual-writes); C-4 `7349a1e` (WACC + EV→Equity bridge + B3 routing flip; delete A1/B1/B2/B3 dual-writes; CalcVersion bump); C-5 (tangible value + currency annotation + docs + closeout). **§8.2.1 Option A mechanism:** dispatcher dual-writes replaced by `applyLedgerComponentDeltas(ctx, working, out)` at `internal/services/datacleaner/adjustments/ledger_apply.go` — applies each fired Restater LedgerEntry's signed `DeltaAmount` to `working.<Component>` (Inventory/OtherIntangibles/DeferredTaxAssets/NormalizedOperatingIncome/InterestExpense) ONLY; NEVER mutates an umbrella (recomputed in `Restated()` at the view level); skips empty-Component OverlayEmitters (A1/B1/B2/B3) + FlagEmitters (C4/C7). **B3 routing flip realized:** WACC reads `Restated().InterestBearingDebt` (B-rule-free); EV→Equity bridge subtracts `InvestedCapital().DebtLikeClaims` (B1+B2+B3) via the NEW `dcf.CalculateEquityValueWithDebtLikeClaims` (6-arg, additive to the legacy 5-arg). The B-rule `dualWrite` closure in `liabilities.go` is now a no-op; A1's `data.Goodwill=0; data.TotalAssets-=goodwill` deleted (effect → `InvestedCapital()`). **KNOWN DEVIATION from plan §8.1:** shadow snapshots are NOT byte-identical — Option A's umbrella-dual-write deletion makes `recomputeUmbrellas` (UNCHANGED shadow shim) observe a different post-clean `data.TotalAssets`, so 9/10 basket shadow snapshots were intentionally regenerated across C-2 (A2/A4/A5) + C-4 (A1). `recompute.go` is untouched; all SEMANTIC invariants GREEN (`TestRecomputeUmbrellas_NoMutation`, `TestOrchestrator_LedgerOrdering`, `TestLedger_BasketSnapshot_ClusterPrediction`, `TestLedger_BasketSnapshot_T2BS3_RestatedReconstruction` AMD $9.679B / KO $60.912B, `NoDoubleCount` pins). `.gitattributes` now pins `recompute-shadow/*.json` to `eol=lf`. **JUDGMENT CALLS:** (a) Graham migrated in C-2 not C-5 — `graham.go` reads `data.TotalAssets`/`TotalLiabilities` directly, so it must move to `AsReported()` in the FIRST cluster that makes umbrellas incoherent to avoid surfacing incoherent umbrellas to the AMD/KO derivation fallback; (b) tangible value reads `AsReported()` not `Restated()` (spec §4.2.12 named Restated on the FALSE premise that `Restated().TangibleAssets` is identity — it actually RECOMPUTES from `TotalAssets − Goodwill − OtherIntangibles`, which would drift `tangible_value_per_share` and break the load-bearing `TestService_calculateTangibleValuePerShare_DilutedDenominator` pin); (c) `CashAndCashEquivalents` not migrated (not a `FinancialDataView` field, never Restater-touched). **DDM consumer migration DEFERRED to Phase 5** — `ddm.go` UNTOUCHED, alt-model `ModelInput` keeps `latestFinancialData.InterestBearingDebt` for `ModelType()=="ddm"`; `TestDDM_LegacyPath_BitForBit` + new `TestDDM_ConsumerPath_UnaffectedByPhase4` GREEN at every commit. **Vestigial-translator + `*AdjustmentResult` struct deletion DEFERRED to Phase 5** — still load-bearing (orchestrator aggregates `result.Applied/.Adjustments/.Flags`). **`Raw()` survives to Phase 5** — zero production `internal/` callers (only a contract test). **Replay verification DEFERRED to operator** — `artifacts/tier2-baseline/2026-05-19/` is `calculation_version 4.1` (pre-Phases-2/3/4 + pre-profile-config), so drift is confounded across phases; a fresh `4.2`/config-current baseline needs live capture. No SchemaVersion bump (FinancialData stays 9; ValuationResult stays 2). Closeout: `docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-4-closeout.md`.
- **DC-1 Phase 5 SHIPPED — PARTIAL merged `e816fcc`, follow-up merged `8ca0841` (2026-06-02); DC-1 (all phases 0–5) is now CODE-COMPLETE and CLOSED.** The ONLY remaining DC-1 item is an OPERATOR follow-up (fresh CalcVersion-4.4 replay baseline capture + live confirmation of the DDM DebtLikeClaims EV correction on a B-rule-firing bank), tracked at `docs/reviewer/DC-1-phase-5-replay-verification-followup.md` — NOT a code blocker. The PARTIAL commit ladder below is retained as history. **PARTIAL (merged `e816fcc`)** — 10 commits, tip `e6418e4` on top of master `b0239ed` (the `dc1-phase-5-prep` spec/plan/handoff merge from 2026-05-28). Ships P5-C1 (DDM EV-bridge DebtLikeClaims correction + `CalculationVersion` 4.3 → 4.4), P5-C2 (DDM consumer migration to `Restated()` view via `LatestRestatedView` with the 4-step bit-for-bit re-proof), P5-C3 SCOPED (orchestrator firing-signal migrated to `nativeFired(...)` helper that filters `LedgerEntry.Fired==true` — full Adjustments-projection deferred), P5-C5 PARTIAL (`cleaneddata.Raw()` deleted + concurrency contract godoc + verify-then-decide on legacy `historicalData` slot → KEEP). **DEFERRED to a follow-up session:** P5-C3-full Adjustments-projection (needs per-AdjusterID metadata table + ARCH decision on `Adjustment.Percentage` handling) and P5-C4 (16 per-rule `*AdjusterOutputToLegacyResult` translators + 3 `*AdjustmentResult` structs + dormant `earnings.go` legacy-fallback helpers deletion). **Commit ladder (10):** `d76be69` P5-C1 (DDM EV-bridge `+DebtLikeClaims` in both legacy Gordon + multi-stage; `modelDebtLikeClaims` populated for DDM in `service.go::performAlternativeValuation`; CalcVersion 4.3 → 4.4 both stamp sites; new `TestDDM_EVBridge_AddsDebtLikeClaims` / `…_ZeroClaims_Unchanged` / `…_GoldenFixtures_ZeroDebtLikeClaims` + 4 `service_test.go::result.CalculationVersion=="4.4"` pins updated); `0535fc5` P5-C2 (DDM reads migrated to `Restated()` view via `input.LatestRestatedView` in `runDividendDiagnostics` + `estimateDividendGrowth`; `service.go` populates `LatestRestatedView` for DDM, nil-branch deleted; 4-step protocol executed in-commit — 2 temporary re-proof tests added → verified → DELETED; `ddm_phase4_invariance_test.go` renamed → `ddm_phase5_invariance_test.go` + `TestDDM_ConsumerPath_UnaffectedByPhase4` → `TestDDM_ConsumerPath_RestatedViewParity` superset pin; DDM's `modelIBD` deliberately STILL on legacy `latestFinancialData.InterestBearingDebt` to minimize the bit-for-bit surface in this commit — IBD view flip is a future tidy-up); `b617407` P5-C5 PARTIAL (`cleaneddata.CleanedFinancialData.Raw()` + its `TODO(phase-5)` marker DELETED at `cleaned.go:79-96`; `service_cleanwithviews_test.go` Raw() assertion deleted; `cleaned.go` godoc on `CleanedFinancialData` strengthened to a HARD request-local invariant per spec §3.6 — explicit "do NOT retrofit sync.Once" rationale documented inline; `historicalData.Data[latestPeriod]` slot population KEPT with `keepLatestCleanedSlot` helper + grep-evidence rationale — 6 remaining `GetLatestPeriod()` consumers across `ddm.go`/`ffo.go`/`revenue_multiple.go`/`currency.go`/`coordinator.go`); `586c370` P5-C3 SCOPED (orchestrator `XResult.Applied` reads at 3 sites in `applyActiveAdjustments` replaced with native firing-signal initially as `len(NativeLedgerEntries) > 0 || ...` inline predicate — INITIAL shipping carried a HIGH-1 bug subsequently fixed in `83e6cb2` below); `2a18a20` Phase 5 partial closeout doc (`docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-5-closeout.md`). **Then 4 post-review fix commits** closing all 9 findings from the gpt-5.5 cross-model review on the Phase 5 partial (zen-mcp continuation `22fbf842`): `83e6cb2` (HIGH-1 — new `nativeFired(entries, overlays, flags)` helper at `internal/services/datacleaner/firing_signal.go` filters `e.Fired==true`; 3 orchestrator call sites migrated; new `TestApplyActiveAdjustments_FiringSignalParity_A1ApplicableButSkipped` regression pin exercises the exact failure path — Goodwill=3% of TotalAssets passes A1 applicability but skips at the 5% threshold, pre-fix predicate would have inflated `RulesApplied` via the diagnostic Fired:false LedgerEntry); `e4ea146` (MEDIUM-2 — replaced misnamed `TestDDM_EVBridge_AddsDebtLikeClaims/multistage_via_payout_path` subtest with `multistage_real` using `testhelpers.BuildSyntheticAAPLishModelInput` + `ResolvedProfile{ArchetypeMaturingTechDividend, DividendForecastHorizon:10}`, asserting `HorizonSelected==10` to prove multi-stage dispatch; MEDIUM-3 — new `TestFFO_IgnoresDebtLikeClaims` Float64bits invariance guard); `b12a870` (MEDIUM-4 spec §3.2 sign-clarity sweep — 5 lines updated; LOW-5 `calculateLegacyGordon` "verbatim" godoc rewritten to enumerate sanctioned Phase 5 deviations + reiterate REVERT-don't-update-goldens rule; LOW-6 `service.go::performAlternativeValuation` "deferred to Phase 5" comment refreshed to current state; LOW-7 18-line inline `historicalData` slot KEEP rationale extracted to `keepLatestCleanedSlot` helper; LOW-8 self-referential `TestCalculationVersion_IsV44` deleted with navigational comment pointing at the 4 live `service_test.go` pins; LOW-9 `// Deprecated:` annotations added to the 3 category-level `*AdjustmentResult.Applied` fields — per-rule `AdjustmentResult.Applied` at `assets.go:284` intentionally NOT annotated, still load-bearing in translators); `de1a456` (REVIEWER LOW close — Phase 5 implementer plan test-name drift at lines 54+58 updated to match shipped `AddsDebtLikeClaims` name); `e6418e4` (gpt-5.5 Q-pass LOW close — refreshed stale firing-signal test docstring at `applyactive_firingsignal_parity_test.go:14-19` that still described the pre-HIGH-1-fix inline predicate). **Full B-V-R-Q with subagents executed on the post-review fix commits:** VERIFIER subagent independently reproduced full suite GREEN + simulated pre-fix predicate to confirm `_A1ApplicableButSkipped` would have failed under the old code; REVIEWER subagent walked the role-vs-emission contract for all 4 adjuster roles (Restater/OverlayEmitter/FlagEmitter/FlagEmitter-fire) and confirmed `nativeFired` is behaviorally equivalent to legacy `Applied`; QA subagent verified per-finding disposition + spec-consistency grep + Deprecated-annotation Go-convention form; gpt-5.5 Q-pass (zen-mcp continuation `bea446b5`) confirmed the role contract holds via grep evidence (zero `Fired:false` literals co-located with `Overlays:` or non-empty `Flags:` in any AdjusterOutput literal) + caught the docstring drift the inline subagents missed. **Load-bearing invariants GREEN at every commit:** `TestDDM_LegacyPath_BitForBit` (JPM/BAC/WFC `math.Float64bits` equality on IntrinsicValuePerShare/EquityValue/EnterpriseValue + Warnings + Confidence — the cross-Tier-2 contract preserved across BOTH P5-C1's `+DebtLikeClaims` term and P5-C2's view migration), `TestDDM_ConsumerPath_RestatedViewParity` (renamed superset pin — output bits + view-equals-entity property for SE/NI/DPS on fixtures), `TestRecomputeUmbrellas_NoMutation`, `TestOrchestrator_LedgerOrdering`, `TestCleanFinancialDataWithViews_Restated_NoDoubleCount_*`, `TestCleanedFinancialData_Restated_C6EquityOffsetZero`, `TestLedger_BasketSnapshot_ClusterPrediction` (10/10), `TestLedger_BasketSnapshot_T2BS3_RestatedReconstruction` (AMD $9.679B / KO $60.912B), shadow snapshots byte-identical (`git diff --quiet internal/integration/testdata/recompute-shadow/` exits 0 at every commit), full `go test ./... -count=1` EXIT=0 (46 packages, 0 FAIL). **DDM-specific sign decision:** DDM derives equity FROM dividends and THEN derives EV (`EV = equity + debt + DebtLikeClaims − cash`), so DebtLikeClaims are ADDED — the OPPOSITE direction from DCF/revenue_multiple which derive equity FROM EV and SUBTRACT it. DDM's `IntrinsicValuePerShare` + `EquityValue` are UNAFFECTED (dividend-derived); only `EnterpriseValue` is corrected (and only for B-rule-firing banks — JPM/BAC/WFC fixtures fire zero B-rules, preserving `+0` term bit-identity). Pinned by new `TestDDM_GoldenFixtures_ZeroDebtLikeClaims`. **Bit-for-bit safety argument for P5-C2 view migration:** SE/NI/DPS are CARRIED in `Restated()` (SE = identityCopy + Σ EquityOffset, NI/DPS pure identity-copied), so when no Restater fires `view.X == latest.X` bit-for-bit; Go local-scalar assignment preserves Float64 bits; IEEE-754 division on identical operand bits is deterministic. Three independent layers pinned. **FOLLOW-UP (merged `8ca0841`, 2026-06-02) — closes the three deferred CODE chunks, completing DC-1.** Branch `dc1-phase-5-followup` (forked from master `974570c`); 7-commit substantive ladder (A0–C1) + 4 review-fix commits (FIX-1..FIX-4) + closeout/findings docs (`fc99d44` closeout + `d7e7443` operator-followup tracker), merged via `8ca0841`. Closed: **P5-C3-full Adjustments-projection** — `result.Adjustments` is now produced by ONE shared native projection `adjustmentsFromLedger(ledger, overlays, perRuleMeta) []entities.Adjustment` at `internal/services/datacleaner/adjustment_projection.go` (walks `data.AdjustmentLedger` in asset→liability→earnings order, looks up a static per-AdjusterID `ruleMeta`, assembles each `entities.Adjustment`); ARCH **Path (a)** preserves `Adjustment.Percentage` bit-for-bit by capturing pre-state on the FIRED `LedgerEntry.SkipMetrics["original_<Field>"]` (`map[string]float64`, NO schema bump) read back with an explicit `ok`-presence check — pinned by `TestPreStateCapture_OnFiredLedgerEntries` (6 sub-pins) + `TestApplyActiveAdjustments_AdjustmentsProjection_BasketParity`. ARCH note: `docs/refactoring/archive/dc1-phase-5-followup-percentage-decision.md`. **P5-C4 translator + struct + dead-helper deletion** — the 16 `*AdjusterOutputToLegacyResult` translators, the dispatcher-side `original*` captures, the dormant `earnings.go` legacy-fallback helpers, the dead `entities.{Asset,Liability}AdjustmentResult` duplicates, and 9 test-only-dead legacy singular `Process*Adjustment` helpers (+ 5 orphaned unit tests) are DELETED; the 3 category `*AdjustmentResult` structs were SLIMMED to the native carrier (`Flags` + `NativeLedgerEntries` + `NativeOverlays` + `NativelyEmittedRuleIDs`) — they remain the live dispatcher return type. KEPT: live category dispatchers; B1/B2 calc engines (`ProcessOperatingLeaseAdjustment`/`ProcessPensionAdjustment`); B3's public `ProcessContingentLiabilityAdjustment` + its private calc engine (supported direct-call entry point). **DDM `modelIBD` view-migration flip** — `service.go::performAlternativeValuation` dropped the `model.ModelType() != "ddm"` exclusion so DDM's `modelIBD` reads `restatedViewOr(cleaned, latestFinancialData).InterestBearingDebt` like every other alt-model; bit-for-bit safe per spec §3.2 NOTE (no Restater touches `InterestBearingDebt`). Validation: full `/execute` B-V-R-Q (VERIFIER+REVIEWER+QA subagents) ×2 (ladder + fix commits) + gpt-5.5 cross-model Q-pass ×2 — no Critical/High blockers; 1 MEDIUM (projection missing-key silent-degradation) + LOWs closed in FIX-1/FIX-2/FIX-3; `/verify` runtime replay confirmed `calculation_version "4.4"` live. **STILL REMAINING (OPERATOR, NOT a code blocker):** fresh CalcVersion-4.4 replay baseline capture + live DebtLikeClaims-EV confirmation on a B-rule-firing bank (the `artifacts/tier2-baseline/2026-05-19/` baseline is `4.1`, so cross-phase drift is confounded) — tracked at `docs/reviewer/DC-1-phase-5-replay-verification-followup.md`. **NEW REALITY (the former "translator stack still load-bearing for `result.Adjustments`" Phase-4 gotcha is now FALSE and RETIRED):** the 16 per-rule translators are DELETED; `result.Adjustments` is produced solely by the native `adjustmentsFromLedger` projection over `data.AdjustmentLedger` — there is no longer any legacy translator read path. CalcVersion stays `"4.4"` / SchemaVersion["FinancialData"] stays 9 (no follow-up bumps — P5-C3-full/P5-C4/IBD-flip are API-behavior-preserving). Follow-up closeout: `docs/refactoring/archive/dc1-phase-5-followup-closeout.md`. Phase 5 PARTIAL closeout: `docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-5-closeout.md`. Phase 5 spec: `docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-5-spec.md`. Phase 5 implementer plan: `docs/refactoring/archive/datacleaner-component-primitive-and-parallel-views-phase-5-implementation-plan.md`. Next-session handoff: `docs/refactoring/implementations/dc1-phase-5-next-session-handoff.md`.
- **Request-valuation-overrides (T1-T10 SHIPPED 2026-06-04; fix iterations + T11 doc-sync SHIPPED 2026-06-05/06).** Every valuation knob resolves through `internal/services/valuation/params/` — `params.Resolve*` functions are the SINGLE authoritative resolution path (config → profile → request override). No handler or model reads config/profile directly for knob values. Handler layer projects `ValuationOverrides` DTO via `projectOverrides` into `params.Overrides` before passing to the service. **Empty-override resolution is byte-identical** — `POST {}` and `GET` responses for the same ticker are guaranteed identical (`TestPostFairValue_EmptyBody_EqualsGET` pins this). **Override contract after fix iterations:**
  - **The override contract computes end-to-end.** Values inside Layer-1 static ranges that also pass Layer-2 always produce HTTP 200 — never a 500. Engine ranges (`pkg/finance/wacc`, `pkg/finance/dcf`) are aligned with the contract ranges.
  - **Dynamic Layer-2 invariants → typed 422 (NOT 500):** (a) `terminal_growth_rate` within 1% of computed WACC → 422 `knob = "terminal_growth_rate"` (`context.limit = WACC − 1%`); (b) CAPM inputs driving WACC ≤ 0 → 422 `knob = "wacc"`; (c) effective horizon > 50 from request-sourced `horizon_years` or `growth_stages` → 422 `knob = "horizon_years"` or `"growth_stages"`.
  - **NaN / ±Inf GET params → 400.** `parseFloatParam` explicitly rejects non-finite values (`override_beta=NaN`, `override_rf=Inf`) with 400 `INVALID_PARAMETER`. Absent param → nil (no error). Unparseable string → error.
  - **Legacy GET ranges widened:** beta [−5, 5], rf [−5%, 25%] — same constants as the `options` Layer-1 validator (`betaMin/betaMax`, `riskFreeRateMin/riskFreeRateMax` in `fair_value_validation.go`).
  - **`terminal_method` is a boolean selector:** `"gordon_growth"` SUPPRESSES exit-multiple blending (pure Gordon TV). `"exit_multiple"` BLENDS 50/50 Gordon/exit average (NOT pure exit-multiple TV). See design spec §13.
  - **Validation asymmetry:** GET `override_beta`/`override_rf` out-of-range or non-finite → **400** `INVALID_PARAMETER`; POST `options` validation → **422** `INVALID_OVERRIDE`. Do NOT change the Layer-1/Layer-2 split.
  - `applied_overrides` echoes **request-set knobs only**; `market_risk_premium` entry echoes the RESOLVED MRP (not raw macroData). `BulkFailure.knob` carries the offending knob name for `INVALID_OVERRIDE` per-ticker failures.
  - **Fix-arc history** (commit ladder + gpt-5.5-pro sign-off): `docs/refactoring/archive/2026-06-06-request-valuation-overrides-fix-iterations-closeout.md`.
- **TDB-1 (#1) — the SEC parser now populates `RestructuringCharges` / `LitigationSettlements` / `CapitalizedInterest`** (shipped on branch `worktree-tdb-1-parser-extraction`, 2026-06-06; `fix(sec)` commit `957be18`). `parsePeriodData` (`internal/infra/gateways/sec/parser.go`) extracts them via `findValue` first-hit candidate lists (us-gaap + `ifrs-full:BorrowingCostsCapitalised`), normalized to a POSITIVE add-back through `absAddBack` (=`math.Abs`) — filers occasionally sign debit-balance charges as credits (JNJ tags `LitigationSettlementExpense` as −379M; without `Abs`, C3's `skip if <= 0` would silently drop a real charge). **Two tags are DELIBERATELY EXCLUDED — do NOT "helpfully" re-add them:** `GainLossRelatedToLitigationSettlement` (credit-balance net GAIN, inverted semantics → mapping it would inflate C3) and `us-gaap:InterestPaidCapitalized` (a cash-flow supplemental disclosure — "cash paid for interest capitalized, investing activity" — a different *measure* than the period capitalized-interest expense C6 reclassifies). Use `findValue` (first-hit), NOT `sumValues` — these are alternative presentations of the same period total. Before TDB-1 these fields were always 0, so C1 fell back to a 1.5%-of-revenue estimate and C3/C6 never fired on real filings; now they feed `NormalizedOperatingIncome`/`InterestExpense`, materiality-gated at the consumer (C1 2%, C3 1% of revenue). New tags were also added to `GetSupportedConcepts()`. `valuation/currency.go` already FX-converts all three (no change needed there). Spec: `docs/refactoring/spec/tdb-1-parser-nonrecurring-extraction-spec.md`; tracker: `docs/reviewer/TDB-1-parser-restructuring-litigation-capex-not-populated.md`.
- **DCF Reinvestment / Operating-Leverage model (valuation-accuracy Layer A) SHIPPED 2026-06-06 — engine now at `CalculationVersion 4.7` (was 4.6 after BUG-015).** `pkg/finance/dcf/dcf.go` gained a unified reinvestment projection branch that replaces the proportional `× growthFactor` CapEx/ΔWC/D&A scaling (which sign-locked projected FCF to the base year) so FCF can cross negative→positive WITHIN the explicit horizon for reinvestment-heavy scalers. **DCF-path ONLY** — DDM/FFO/revenue_multiple are bit-for-bit unchanged (only `calculation_version` drifts on those paths). The new path is gated by `useReinvestmentModel(inputs)`: it engages ONLY when the resolved `AssumptionProfile.ReinvestmentMethod` is `sales_to_capital`/`declining_capex_intensity` AND a TTM revenue base exists; otherwise (empty/`legacy_proportional` method, or no revenue) the LEGACY proportional path runs byte-identical (pinned by `TestCalculateDCF_LegacyProportional_BitForBit`). 7 growth profiles opt in via `config/assumption_profiles.json` (`config_version 1.1.0`): `cyclical_mid_cycle:high_growth` (AMD/NVDA), `hypergrowth_early/profitable:high_growth`, `software_like_scaling:high_growth`, `software_like_large_scale:{mature,standard_growth,high_growth}` — every OTHER profile stays legacy. Math: `Reinvestment_t = ΔRevenue_t / SalesToCapital_t` (efficiency rising), `NOPAT_t = Revenue_t × margin_t × (1−tax)` with a base→target margin-convergence path, `FCF_t = NOPAT_t − Reinvestment_t`. **§7.3 terminal consistency:** terminal reinvestment = `Revenue_{n+1} × g / SalesToCapitalTarget` ⇒ terminal RR = `g/ROIC` EXACTLY; the §7.1 maintenance-capex floor is an EXPLICIT-window guardrail ONLY (NOT applied at the terminal — a terminal floor double-counts maintenance in the net-reinvestment frame and breaks the g/ROIC identity). `service.go::applyReinvestmentModel` (in `internal/services/valuation/reinvestment.go`) derives the params from the resolved profile + `TrailingTwelveMonthsRevenue()` and applies a `max(base,target)` margin guard (only EXPAND margins, never contract — protects high-margin firms like NVDA); it emits a `reinvestment_model:` audit tag on `result.Warnings` + a `reinvestment_model` calc-trace stage. Validation invariants live in `profile/validation.go::validateReinvestmentFields` (method-aware: empty method == legacy, never rejected; `target ≥ start > 0`; `MaintenanceCapexFloor < TargetOperatingMargin`; `TargetOperatingMargin ≤ 0.60`). Live validation (`cmd/accuracy` 4.6→4.7, 10-ticker basket): AMD intrinsic 6.97→112.57, per-year FCF crosses positive at yr 2 (NEG_FCF_YEARS 5→1), terminal share 247%→89%; mean absolute basket gap 59.1%→53.2%; NVDA/AAPL/MSFT gaps all narrowed. New 4.7 baseline at `artifacts/tier2-baseline/2026-06-06/` + report `docs/accuracy/report-2026-06-06.md`. Spec: `docs/refactoring/spec/dcf-reinvestment-and-filing-intelligence-spec.md` (Layer A = Phase 1 SHIPPED; Layers B/X = Phases 2-4 PLANNING). Closeout: `docs/refactoring/archive/dcf-reinvestment-layer-a-closeout.md`. **Distinct stream from DC-1** — built on top of BUG-014/BUG-015; does not re-open them.
- **Layer B Phase 2 — guidance-artifact fixture consumption + assumption-authority hierarchy SHIPPED 2026-06-08 (on top of Layer A; CalculationVersion UNCHANGED at 4.7).** midas can consume a hand-authored, immutable, content-addressed guidance fixture deterministically (NO LLM yet) and anchor **near-term (year 1–2) ONLY** DCF inputs. **MASTER INVARIANT (NF1): production runs with empty `config.Valuation.GuidanceRoot` ⇒ the loader returns Absent immediately ⇒ every valuation is BYTE-IDENTICAL to the 4.7 engine** (this is why DDM/FFO/revenue_multiple + all un-fixtured tickers stay bit-for-bit, and why CalcVersion does NOT bump). New packages: `internal/services/valuation/guidance` (artifact types + canonical SHA-256 hashing + structural validation + accession-keyed immutable `Loader`) and `internal/services/valuation/authority` (the §9 5-level precedence resolver + §9.3 guardrails). Flow: `service.go::performValuation` calls `resolveGuidance` (in `internal/services/valuation/guidance.go`) AFTER profile resolution; it loads (by `(CIK, as-of=Clock.Now())`), resolves precedence, captures a `09-guidance.json` replay stage, and threads `authority.NearTermAnchors` into `reinvestment.go::applyNearTermAnchors`. Anchors are applied via the ADDITIVE engine seams `dcf.Inputs.NearTermReinvestmentOverride` + `NearTermMarginOverride` (`map[int]float64`, year→value; nil/empty ⇒ byte-identical; `validateInputs` rejects year ∉ [1,2]). §9.3 guardrails (resolver): numeric override requires `status==validated` + `confidence≥0.70` + explicit `*float64` value + ≥1 evidence quote; vague-prose ⇒ context-only; **near-term-only is STRUCTURAL** (the `NearTermAnchors` type has only Year1/Year2 fields + `RefuseAnchorIndex`/`assertNearTermPrefix`). §12.4 anchor mechanic = **midpoint-replace** `m=(low+high)/2`, year 1–2 only — guidance can NEVER touch the terminal (the dominance guardrail). Loader: sorted scan, content-hash HARD error (verified BEFORE structural validation), conflict total-order (newest filing_date → form rank → accession-lex, same-period only), newest-period staleness, issuer-CIK-matches-dir, kind-specific units (capex=`absolute_usd`, margin/revenue=`pct`). **CIK is normalized via `normalizeCIK` (zero-pad to 10 — SEC serializes un-padded e.g. AMD `2488`; this was a live-caught bug — AND the path-traversal guard).** Response: `FairValueResponse.assumption_sources` (omitempty; per-assumption `{source, detail}`) + a `guidance:` Warnings source tag. Replay: `BundleGuidanceGateway` reads captured `09-guidance.json` bytes (absent-not-panic for old bundles); any NEW `FairValueResponse` field must be registered in `internal/observability/replay/diff.go` (`goFieldToJSON` map + `countFairValueFields`) or the `init()` field-count-drift guard panics. **Validated via 4 rounds: B-V-R-Q role subagents → live API testing (caught 2 integration bugs the all-green unit suite missed: the un-padded CIK + the missing response field) → zen-mcp gpt-5.5 CROSS-MODEL review (caught 11 more incl. a §9.3 terminal-leak HIGH where the year-1 margin anchor mutated `BaseOperatingMargin`/`TargetOperatingMargin`, now routed through `NearTermMarginOverride[1]`) → live re-confirm.** **Phase 3 DEFERRED:** the gross→net CapEx conversion (`net=grossCapEx−D&A+ΔWC`; today guided gross capex is treated AS net reinvestment, disclosed via `gross_capex_as_net_reinvestment_approx=true`), the offline LLM extraction tool, flipping `GuidanceRoot` to a production dir (+ the CalcVersion bump that first production value-change warrants). Spec: `docs/refactoring/spec/layer-b-phase2-guidance-fixture-spec.md`. Closeout: `docs/refactoring/archive/layer-b-phase2-guidance-fixture-closeout.md`.
- **TDB-3 (#3) — the B3 contingent-liability AI-FAILED fallback uses the industry heuristic, NOT a flat 0.40** (`adjustments/liabilities.go`, 2026-06-08). When `enable_ai_integration` is on and `analyzeContingentLiabilityWithAI` FAILS, both fallback arms of `processContingentLiabilityAdjustment` call `getContingentLiabilityProbability(IndustryCode, total)` (sector-calibrated: 45→0.40, 20→0.70, 25→0.65, 21→0.60, 62→0.50, default→0.30) — the SAME policy as the AI-disabled `default` arm (previously AI-failed used an arbitrary flat 0.40). **Do NOT re-introduce a magic constant on AI failure.** `AIProvenance` stays nil on failure; the single-AI-call invariant (DC-1 Phase 3 HIGH-2/3) + SHA-256 provenance on success are unchanged; the `contingent_liabilities:` reasoning prefix is preserved. **Inert under default config** — `enable_ai_integration` defaults false, so the AI-failed arms are unreachable in production (the heuristic runs via the `default` arm); DDM bit-for-bit, recompute-shadow, and ledger-basket suites are all AI-disabled and untouched. Pinned by `b3_ai_fallback_test.go::TestB3ContingentLiability_FallbackPolicy` (the decisive AI-failed subtests use non-Tech codes — `20`→0.70, `21`→0.60 — so they fail against the old 0.40). Spec: `docs/refactoring/spec/tdb-3-contingent-liability-ai-fallback-spec.md`.
